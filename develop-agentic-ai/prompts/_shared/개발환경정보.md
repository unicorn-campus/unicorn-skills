# 개발 환경 정보 기입란 — 개발 프롬프트 9종 공용 입력

> 개발 프롬프트 9종(`01-runtime.md` ~ `09-eval.md`)이 **공통으로 읽는 단 하나의 입력 파일**임.  
> 프롬프트를 실행하기 **전에** 이 파일을 먼저 채움. 여기가 비면 프롬프트가 시작 지점에서 막힘.

**이 파일이 설계서와 다른 점** — 설계서는 "무엇을 만들 것인가"를 정함.  
이 파일은 **"무엇으로 만들 것인가"**를 정함. 설계서에 없는 값만 여기에 둠.

---

## 1. 쓰는 법

- 이 파일을 복사하지 않고 **여기에 직접 값을 적음**. 프롬프트가 이 경로를 그대로 읽음
- 값을 모르면 빈칸으로 두지 않고 `프로젝트 확정 필요`라고 적음
- 값을 적은 행마다 **정한 사람과 날짜**를 함께 적음. 팀이 정한 값이면 `개발판단`이라고 적음

**차단 규칙** — 어떤 프롬프트를 실행할 때 그 프롬프트가 쓰는 변수가 `프로젝트 확정 필요`로
남아 있으면 프롬프트는 **코드를 만들지 않고 사용자에게 문의함.** 비운 채 진행하지 않음.

**설계서에 있는 값을 여기에 다시 적지 않음** — 모델 벤더·시간 상한·반복 상한·저장소 제품은
설계서가 주인임. 여기에 또 적으면 두 곳이 어긋났을 때 어느 쪽이 진짜인지 모르게 됨.

| 여기에 적는 것 | 설계서가 주인이라 여기에 안 적는 것 |
|---------------|--------------------------------|
| 언어 · 프레임워크 · 디렉터리 · 시험 도구 | 모델 벤더 · 모델명(④ 「사용 모델」) |
| 상태 저장 백엔드 제품 · 배포 런타임 제품 | 시간 상한 · 재시도 횟수 · 반복 상한 · State 필드 이름 · 프로세스별 API 키 이름(③) |
| 환경변수 이름 규격 | 실행환경 묶음(⑦ 물리 배치도) · 저장소 제품과 색인 파라미터(⑤ 3-1) |
| 관측 기록 규격 | 기록 항목 · 재시도 간격 · 상한 · 알림 임계(⑥) · 가릴 필드(⑤ `F-n` · ⑥ 「마스킹」) |

**⑦ 배포는 물리 배치도 1장이 전부임** — 포트 · 비밀값 목록 · 배포 형태 · 규모 상한 ·
저장소 안/밖 배치 · 접속정보는 **설계 범위 밖**이라 어느 설계서도 갖지 않음.
그 값의 주인은 개발 프롬프트임: **포트는 `07-api-ui.md`**, **비밀값 목록과 나머지는
`08-deploy.md`**가 되묻기로 정함. **여기에도 적지 않음**(제품 종류를 고르는 칸만 여기 둠).

---

## 2. 변수 12종과 쓰이는 곳

| 변수 | 무엇인가 | 이 값이 없으면 막히는 프롬프트 |
|------|---------|------------------------------|
| `D-01` | 언어 · 런타임 버전 | 9종 전부 |
| `D-02` | 흐름 조립 프레임워크 | 1 runtime · 6 workflow |
| `D-03` | 웹 프레임워크 · API 규격 | 7 api-ui |
| `D-04` | 프론트엔드 스택 | 7 api-ui |
| `D-05` | 코드 루트 경로 · 디렉터리 규칙 | 9종 전부 `[출력]` |
| `D-06` | 패키지 매니저 · 의존성 파일 | 9종 전부 |
| `D-07` | 시험 도구 · 실호출 시험 분리 표식 | 9종 전부 완료조건 |
| `D-08` | 상태 저장 · 중간 저장 장치 백엔드 | 1 runtime · 6 workflow |
| `D-09` | 컨테이너 · 배포 런타임 | 8 deploy |
| `D-10` | 비밀값 주입 **수단**(항목 목록은 `08-deploy.md` 4단계 소유) | 8 deploy |
| `D-11` | 관측 백엔드 · 내보내기 방식 · 알림 발신 수단 | 5 guardrail |
| `D-12` | 환경변수 이름 규격 | 1 runtime(정하면 9종이 따름) |

---

## 3. 기입란

### D-01 언어 · 런타임 버전

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 주 언어 · 버전 | Python 3.12 | 사용자 지정 · 2026-08-08 |
| 부 언어(있으면) · 버전 | TypeScript 5.x(프론트엔드) | 클로니 대체 도출 · 2026-08-08 |
| 왜 이 버전인가 | LangGraph·주요 AI 라이브러리가 3.12를 기준으로 배포됨. 3.13 전면 전환은 의존성 성숙도 확인 전이라 보류 | 개발판단 |

### D-02 흐름 조립 프레임워크

**어느 설계서도 이 값을 갖지 않음**(설계 범위 밖). 여기가 유일한 주인임.

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 프레임워크 · 버전 | LangGraph(최신 안정판 — 코드 작성 직전 context7 MCP로 버전 재확인) | 사용자 지정 · 2026-08-08 |
| 프레임워크를 쓰지 않고 직접 짜나 | 아니오 | |

### D-03 웹 프레임워크 · API 규격

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 웹 프레임워크 · 버전 | FastAPI(최신 안정판) | 클로니 추천 · 2026-08-08 |
| API 규격(REST · GraphQL · gRPC 등) | REST | 클로니 추천 |
| 명세 문서 생성 방식 | FastAPI 자동 OpenAPI(Swagger UI) | 클로니 추천 |
| 부분 전송(스트리밍) 수단 | SSE(Server-Sent Events) | 클로니 추천 — 추천 생성·챗형 응답의 토큰 단위 전송에 맞춤 |

### D-04 프론트엔드 스택

**화면이 없는 프로젝트면 `해당 없음`이라고 적음.** 그러면 `07-api-ui.md`가 API만 만듦.

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 프레임워크 · 빌드 도구 | React + Vite | 사용자 지정(React) · 클로니 대체 도출(Vite) · 2026-08-08 |
| 언어 · 타입 검사 | TypeScript(strict 모드) | 사용자 지정 · 2026-08-08 |
| 패키지 매니저 | pnpm | 클로니 추천 |

### D-05 코드 루트 경로 · 디렉터리 규칙

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 코드 루트 | `output/src/v1.2` | 사용자 지정 · 2026-08-09 |
| 공통 모듈 위치 | `output/src/v1.2/common`(상태 타입·설정·모델 클라이언트 등 01-runtime 산출물) | 클로니 대체 도출 |
| 공용 도구 계층 위치 | `output/src/v1.2/tools`(커넥터 어댑터 공용 바탕 — 도구 정의·오류 분류·재시도 감싸개·승인·중복 방지 키. 배포 단위 아님, 서비스가 가져다 쓰는 라이브러리) | 04-connector.md 되묻기 5번 · 클로니 승인 · 2026-08-09 |
| 서비스별 모듈 위치 | `output/src/v1.2/services/{member-service, recommendation-history-service, payment-service, daily-learning-batch}` | 클로니 대체 도출 — 설계서 ② 「내부 관계도」 노드명(회원 서비스·추천·이력 서비스·결제 서비스·일일 취향 학습 배치)을 영문 슬러그로 1:1 대응 |
| 프론트 위치 | `output/src/v1.2/frontend` | 클로니 대체 도출 — 「내부 관계도」 노드명 `프론트엔드` 대응 |
| 시험 코드 위치 | 서비스별 `tests/`(각 모듈 하위) + 루트 `output/src/v1.2/tests/e2e`(서비스 간 시험) | 클로니 추천 |
| 설정 · 매니페스트 위치 | 각 모듈 루트의 `pyproject.toml` / `frontend/package.json` + 루트 `output/src/v1.2/docker-compose.yml` | 클로니 추천 |

**디렉터리 이름은 설계서 ②의 「내부 관계도」 노드 이름과 맞춤.** 다른 이름을 새로 짓지 않음.  
②가 「프로세스 분리표」로 프로세스를 나눴으면 그 경계도 함께 반영함.

### D-06 패키지 매니저 · 의존성 파일

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 패키지 매니저 | uv(Python) · pnpm(프론트엔드) | 클로니 추천 · 2026-08-08 |
| 의존성 파일명 | `pyproject.toml`(서비스별) · `frontend/package.json` | 클로니 추천 |
| 가상환경 · 격리 방식 | uv가 관리하는 `.venv`(서비스별 독립) | 클로니 추천 |
| 버전 고정 방식 | `uv.lock` · `pnpm-lock.yaml` 커밋 | 클로니 추천 |

### D-07 시험 도구 · 실호출 시험 분리 표식

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 단위 시험 도구 | pytest(백엔드) · Vitest + React Testing Library(프론트엔드) | 클로니 추천 · 2026-08-08 |
| 대역(Mock) 도구 | pytest-mock · `unittest.mock`(외부 API·PG·지도·식약처 Mock 커넥터) | 클로니 추천 |
| 실호출 시험 분리 표식 | pytest 마커 `@pytest.mark.live_call`(기본 실행 제외, `-m live_call`로만 실행) | 클로니 추천 |
| 커버리지 목표(있으면) | 해당 없음 — 수치 목표는 지어내지 않음 | |

### D-08 상태 저장 · 중간 저장 장치 백엔드

중간 저장 장치 = 흐름이 중간에 끊겨도 이어서 할 수 있게 상태를 저장해 두는 장치임.

**`세션 격리 키 성분`은 ③ 「체크포인트 보존」이, `보관 기간`은 ⑤ 3-3 「보존·삭제」의 체크포인트
행이 소유함**: 여기에 원본을 두지 않고 **인용만** 함. **`저장 등급`과 백엔드 제품은 설계 범위 밖**
(⑦ 배포는 물리 배치도만 그리고 저장 등급을 정하지 않음)이라 **이 절이 주인임**.

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 중간 저장 방식 | 데이터베이스 | 사용자 지정 · 2026-08-08 |
| 저장 등급 | 공유 트랜잭션 저장(인스턴스가 여럿이라 단일 노드 저장을 운영 기본값으로 두지 않음) | 이 절 소유(설계 범위 밖) |
| 백엔드 제품 · 버전 | PostgreSQL(최신 안정판) | 사용자 지정(PostgreSQL) |
| 체크포인터 구현체 | LangGraph `AsyncPostgresSaver`. **바꿔 끼울 수 있게 한 겹 감쌈**(개발은 메모리) | 클로니 도출(프레임워크 종속 값이라 이 절 소유) |
| 세션 식별자 문자열 문법 | ③ 「체크포인트 보존」의 `세션 격리 키 성분`을 `:`로 이어 붙임. **성분을 더하거나 빼지 않고 실행 시각을 넣지 않음** | 이 절 소유(문법만) |
| 보관 기간(인용) | ⑤ 3-3 「보존·삭제」의 체크포인트 행에서 읽음. ③의 `스레드 최소 생존 시간`보다 짧게 잡지 않음 | ⑤ 소유 |
| 만료 삭제 작업 | 프레임워크에 만료 기능이 없어 `08-deploy.md` 9단계가 파기 작업을 만듦 | 클로니 도출 |

### D-09 컨테이너 · 배포 런타임

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 이미지 빌드 도구 | Docker | 사용자 지정 · 2026-08-08 |
| 기반 이미지 · 고정 방식 | `python:3.12-slim`(백엔드) · `node:22-alpine` 빌드 후 정적 자원만 배포(프론트엔드) — 다이제스트 고정은 배포 시점에 확정 | 클로니 추천 |
| 로컬 실행 방식 | `docker compose up`(루트 `docker-compose.yml`) | 클로니 추천 |
| 배포 대상 런타임 | `[확인필요: 배포 대상 컨테이너 오케스트레이션 런타임]` — **어느 설계서도 이 값을 갖지 않음**(설계 범위 밖). `08-deploy.md`는 이미지·로컬 실행까지만 다루고 실제 배포 타깃은 별도 확인 | |

### D-10 비밀값 주입 수단

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 로컬 개발 주입 방식 | `.env` 파일 + `pydantic-settings`(백엔드) · Vite `.env.local`(프론트엔드) | 클로니 추천 · 2026-08-08 |
| 배포 환경 주입 방식 | 플랫폼 비밀 객체 → 컨테이너 환경변수(실행 시 주입, 이미지에 굽지 않음) — `08-deploy.md` 7단계 「위반 3종」 원칙과 동일 | 클로니 추천 |
| 예시 파일명 | `.env.example`(서비스별) | 클로니 추천 |
| 저장소에 올리지 않을 파일 목록 | `.env` · `.env.local` · `*.local.env` · `uv.lock`은 커밋 대상(제외 아님) | 클로니 추천 |

### D-11 관측 백엔드 · 내보내기 방식

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 추적 기록 규격 | OpenTelemetry(OTLP) — 설계서 ⑥이 요구하는 분산 추적·LLM 호출 트레이스를 표준 규격으로 수집 | 클로니 추천 · 2026-08-08 |
| 내보내기 대상 · 방식 | `[확인필요: 관측 백엔드 제품]` — **어느 설계서도 이 값을 갖지 않음**(⑥ 「관측 적재처」는 `저장소 유형`까지만 정하고 제품·전송 키는 설계 범위 밖). OTLP exporter로 개발해 제품 교체 가능하게 둠 | |
| 로그 형식 | 구조화 로그(JSON), 표준 `logging` + JSON 포매터 | 클로니 추천 |
| 알림 발신 수단 | 표준출력 로그 1줄 + 갈아 끼울 수 있는 발신 어댑터 — ⑥ 「알림 임계값」은 `알릴 대상`(누구)까지만 정하고 발신 수단은 설계 범위 밖임 | 클로니 추천 |
| 로컬 개발 시 대체 방식 | 콘솔 표준출력(stdout)으로 JSON 로그·OTLP 콘솔 exporter 출력 | 클로니 추천 |

**제품이 안 정해졌으면 `프로젝트 확정 필요`로 두고 진행함.** `05-guardrail.md`가 내보내기 부분을
갈아 끼울 수 있게 만들고 제품 이름은 설정으로 뺌.

### D-12 환경변수 이름 규격

| 항목 | 값 | 정한 사람 · 날짜 |
|------|----|----------------|
| 접두 문자열 | `{프로젝트영문}` | 클로니 추천 · 2026-08-08 |
| 이름 짜는 규칙 | `{접두}_{영역}_{항목}`(기본값) | 클로니 추천 |
| 대소문자 · 구분자 | 대문자 · 밑줄(기본값) | 클로니 추천 |

**이 값을 안 정하면 프롬프트 9건이 제각각 이름을 지음.** `01-runtime.md`가 가장 먼저 씀.

---

## 4. 기입 완료 점검

프롬프트를 돌리기 전에 아래를 확인함.

- [ ] `D-01` ~ `D-12` 중 이번에 돌릴 프롬프트가 쓰는 변수가 전부 채워졌는가
- [ ] `프로젝트 확정 필요`로 남은 행이 몇 개인지 숫자로 셌는가
- [ ] `D-05` 디렉터리 이름이 설계서 ②의 「내부 관계도」 노드 이름과 어긋나지 않는가
- [ ] 설계서가 주인인 값(모델 · 시간 상한 · 재시도 횟수 · 저장소 제품 ·
      **세션 격리 키 성분(③) · 보관 기간(⑤) · 재시도 간격·상한·알림 임계(⑥)**)을 여기에
      중복해 적지 않았는가
- [ ] 개발 프롬프트가 주인인 값(**포트는 `07-api-ui.md` · 비밀값 항목 목록과 배포 형태·규모
      상한은 `08-deploy.md`**)을 여기에 중복해 적지 않았는가
- [ ] `D-04`가 `해당 없음`이면 화면 관련 항목을 전부 `해당 없음`으로 맞췄는가

### 미확정 항목

| # | 무엇이 없나 | 누구에게 되묻나 | 안 오면 무엇이 막히나 |
|---|-----------|---------------|--------------------|
| 1 | 체크포인트 보관 기간(**⑤ 3-3 「보존·삭제」 소유**. `D-08`이 정하지 않음) | ⑤ 지식/도구를 고치는 설계자 → 못 정하면 사용자 | `01-runtime.md`의 만료 삭제 규칙과 `08-deploy.md` 9단계 파기 작업 — 없어도 저장 자체는 진행하되 삭제 배치는 보류(**프레임워크에 만료 기능이 없어 방치하면 영구 보존됨**) |
| 2 | `D-09` 배포 대상 컨테이너 오케스트레이션 런타임(**설계 범위 밖**이라 어느 설계서에도 없음) | 사용자 | `08-deploy.md`의 배포 매니페스트 제품별 문법 — 이미지 빌드·로컬 실행까지는 진행 가능 |
| 3 | `D-11` 관측 백엔드 제품(**설계 범위 밖**. ⑥은 `저장소 유형`까지만 정함) | 사용자 | `05-guardrail.md`의 실제 내보내기 대상 — OTLP 개발은 진행하고 제품 연결만 보류 |
