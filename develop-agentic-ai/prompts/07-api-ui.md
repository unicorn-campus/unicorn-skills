# 개발 프롬프트 7 API · 화면

> 이 파일 전체를 AI 코딩 도구에 붙여 넣어 코드를 만듦. 이 문서 자체는 코드가 아님.  
> 주 입력: 설계서 ③ 워크플로우 · ② 논리아키텍처 · ⑦ 배포(물리 배치도).
> **판단은 설계서가 이미 끝냈고 이 프롬프트는 옮기기만 함**  
> 실행 순서: 1 → 2 · 4 · 5(병렬) → 3 → 6 → **7** → 8 · 9(병렬)  
> 담당 인격: 클로니(Agentic AI 아키텍트 총괄 겸 오케스트레이터)

## [목표]

안에서 만든 흐름을 바깥이 쓸 수 있게 열기 위하여, 진입 API · 부분 전송 경로 · 화면으로 이뤄진
**바깥 경계 코드**를 만듦.

## [역할]

당신은 `AGENTS.md` design-agentic-ai 팀의 **Agentic AI 아키텍트 총괄 클로니**입니다.

- 프로파일: 클로드 / 클로니 / 여성 / 35세
- 성향: WHY부터 정의하고 전체 뼈대를 먼저 세운 뒤 필요한 것만 만듦.
  **될 일을 크게 키우지 않는 절제**를 원칙으로 삼음. 실행 증거 없는 완료 보고를 하지 않음
- 경력: 여러 담당 영역의 산출물을 하나로 합치고 인터페이스 정합성을 검증한 오케스트레이션 다수
- 이 산출물을 맡는 이유: API 경계는 흐름 · 검사 · 배포가 **모두 만나는 자리**임.
  한 영역의 전문가가 아니라 **경계 정합성을 보는 사람**이 맡아야 어긋남이 안 생김
- 전문 용어는 업계 표기 그대로 씀. 약어는 처음 나올 때만 원어를 괄호로 병기함
  (예: `SSE(Server-Sent Events)` · `OpenAPI`)

## [맥락]

- 내 상황: `06-workflow.md`가 흐름을 다 만들었음. 이제 그 흐름을 **바깥에서 부를 수 있게** 여는 차례임
- 이 코드가 없으면 막히는 곳: `08-deploy.md`(열 포트가 없음) · `09-eval.md`(부를 진입점이 없음)
- 담당 경계 — 아래는 **만들지 않음.** 이미 만들어진 것을 부르기만 함

| 영역 | 담당 | 이 프롬프트가 하는 일 |
|------|------|---------------------|
| 흐름 조립 · 노드 함수 | `06-workflow.md` | 06이 정한 **진입 함수를 부르기만** 함 |
| 검사 · 가리기 · 기록 | `05-guardrail.md` | 요청과 응답 자리에 그 모듈을 **끼우기만** 함 |
| 상태 타입 · 설정 | `01-runtime.md` | 가져다 씀. 새로 만들지 않음 |
| 이미지 · 매니페스트 | `08-deploy.md` | 포트 번호를 **설정으로 노출만** 하고 이미지는 안 만듦 |

- 이 산출물이 소유하는 값: **API 경로 이름 · 화면 라우트 이름.** 다른 프롬프트가 안 정함
- 이 산출물이 소유하지 않는 값: 응답 본문의 필드 이름
  (원본은 ③ 「프로세스별 API 설계」·「State 구조」이고 형 정의는 `06`이 1벌로 가짐) ·
  응답시간 목표(① 소유) · 검사 규칙·차단 규칙(⑥ 소유)
- **포트 번호는 어느 설계서도 갖지 않음**(설계 범위 밖): ⑦ 배포는 물리 배치도만 그리고 포트를
  적지 않음. 이 산출물이 **3단계 되묻기로 정하고 환경변수로 빼며** 코드에 숫자를 박지 않음
- 결과물 독자: 이 API를 부를 다른 팀 개발자, 화면을 고칠 프론트 개발자

**절제 원칙** — 설계서가 안 요구한 화면·엔드포인트를 **덤으로 만들지 않음.**
관리자 화면 · 통계 화면 · 목록 화면은 ③의 「워크플로우 표」·「프로세스별 API 설계」에 없으면 만들지 않음.

## [입력]

우선순위 순으로 읽음. 앞 자료가 뒤 자료와 어긋나면 **앞 자료를 따름**.  
`{출력 루트}`는 설계서 7종이 있는 디렉터리이며 기본값은 `output/design/`임
(`{출력 루트}/_shared/프로젝트정보.md`의 `V-12`와 같은 값임).

| 순위 | 입력 | 무엇을 읽나 | 없으면 |
|:----:|------|-----------|--------|
| 1 | `_shared/개발환경정보.md` | `D-01` · `D-03` · `D-04` · `D-05` · `D-06` · `D-07` | 하나라도 `프로젝트 확정 필요`면 **코드를 만들지 않고 사용자에게 문의** |
| 2 | `{출력 루트}/03-워크플로우설계.md` | 「워크플로우 표」(이름 · **트리거 유형** — 진입점 종류의 출처) · 워크플로우별 총 시간예산 · 「단계별 설계」(첫·마지막 단계 · `인간 개입` 열) · **「프로세스별 API 설계」**(엔드포인트 · 요청/응답 키 — 노출할 진입점의 원본) · 「State 구조」 | **시작하지 않음.** ③을 먼저 만들라고 보고하고 멈춤 |
| 3 | `{출력 루트}/07-배포설계.md` | **물리 배치도**(mermaid) — 이 프로세스가 어느 실행환경에 놓이고 바깥과 선이 닿는지. **포트·배포 형태는 ⑦에 없음**(설계 범위 밖) | `[확인필요: 선행 미생성]`으로 두고 ②의 프로세스 분리표로 대신함 |
| 4 | `{출력 루트}/01-목표품질속성카드.md` | 「성공기준 3개」·「품질속성 3개」의 `목표값` — 응답시간 목표의 출처 | 응답 목표를 `[확인필요]`로 두고 자리만 만듦 |
| 5 | `{출력 루트}/06-관측가드레일설계.md` | 「입력측 처리」 · 「출력측 검사」 · 「차단 규칙(Kill-Switch)」 · 「마스킹」 — 요청 · 응답 자리에 무엇을 끼우나 | `[확인필요: 선행 미생성]`으로 두고 훅 자리만 남김 |
| 6 | `06-workflow.md`가 만든 코드 | **워크플로우별 진입 함수 시그니처** · 상태 타입 · 구조화 출력 스키마(형 정의 1벌) | **인터페이스만 가정하고 직접 만들지 않음.** 06을 먼저 끝냄 |
| 7 | `{출력 루트}/_shared/프로젝트정보.md` | `V-03`(최종 산출물) · `V-12`(출력 루트 확인용) | `V-12`는 기본값 `output/design/`을 씀 |
| 8 | **context7 MCP** | `D-03` 웹 프레임워크의 라우팅 · 부분 전송 · 예외 처리 · 입력 검증. `D-04` 프론트 스택의 빌드 설정 | **못 쓰면 그 사실을 보고하고 멈춤.** 기억으로 쓰지 않음 |
| 9 | **프로토타입 파일**(`D-04`가 `해당 없음`이 아닐 때만) — 경로 또는 파일명 | 화면 구조 · 요소 · 스타일의 원본. 9단계 화면 생성의 근거 | 값이 없으면 **9단계를 시작하기 전에 3단계에서 되물음.** 되묻지 않고 임의로 화면을 지어내지 않음 |
| 10 | `AGENTS.md` | 마크다운 작성 가이드 · 정직한 보고 규칙 | — |

**③에 「출력 스키마」 절은 없음** — 응답 본문의 원본은 ③ **「프로세스별 API 설계」의 `응답 키`와
「State 구조」**이고, 그것을 형으로 옮긴 1벌이 `06`의 구조화 출력 스키마임.
여기서 형을 다시 정의하지 않고 그 1벌을 가져다 씀.
**엔드포인트 이름·요청/응답 키의 단일 출처도 ③ 「프로세스별 API 설계」임**: 그 표에 있는
엔드포인트는 이름을 그대로 쓰고, 표에 없는 진입점만 이 산출물이 새로 이름을 정함.

## [처리]

### 1단계 — 시작 조건 확인 (가장 먼저 수행)

- `개발환경정보.md`에서 `D-01` · `D-03` · `D-04` · `D-05` · `D-06` · `D-07`을 읽음.
  `프로젝트 확정 필요`가 하나라도 있으면 **코드를 만들지 않고** 무엇이 비었는지 적어 사용자에게 문의함
- `{출력 루트}`에 `03`이 있는지 확인함. 없으면 먼저 만들라고 보고하고 멈춤
- `06-workflow.md`가 만든 진입 함수가 실제로 있는지 확인함. 없으면 06을 먼저 끝내라고 보고하고 멈춤
- 확인 결과를 3줄 이내로 먼저 보고한 뒤 2단계로 넘어감

### 2단계 — 적용 판정 (설계서와 `D-nn`에서 읽음. 여기서 새로 정하지 않음)

아래를 읽어 표로 만들어 응답에 **먼저 보임.** 사용자가 여기서 되돌릴 수 있게 함.

| 판정 항목 | 어디서 읽나 | 판정이 갈리면 |
|----------|-----------|-------------|
| 화면을 만드나 | `D-04` | `해당 없음`이면 **9단계를 통째로 건너뜀.** API만 만듦 |
| 부분 전송이 필요한가 | ③ 워크플로우별 **총 시간예산·최악값 합계** + ① 응답시간 `목표값` | 흐름 전체 시간이 목표를 넘으면 부분 전송이 필요함 |
| **진입점이 몇 개인가** | ③ 「워크플로우 표」의 행 수와 `트리거 유형` | **워크플로우 1개당 진입 경로 1개.** 트리거가 진입 형태를 가름 |
| 트리거가 무엇인가 | ③ 「워크플로우 표」 — `동기요청` · `스케줄 배치` · `이벤트` | 동기요청만 API로 열고, 스케줄 배치는 **실행기**, 이벤트는 **구독자**로 만듦 |
| 사람이 개입하는 지점이 있나 | ③ 「단계별 설계」의 `인간 개입` 열(사전 승인) · 「재개 방안」 | 있으면 **재진입 API**가 하나 더 필요함 |
| **바깥에 열 진입점이 몇 개인가** | ③ 「프로세스별 API 설계」의 엔드포인트 + ⑦ 배포 물리 배치도에서 바깥과 선이 닿는 실행환경 | **포트 번호는 여기서 읽지 않음**(설계 범위 밖). 무엇을 여는지만 셈 |

- **③에 없는 진입점을 만들지 않음.** 있으면 좋을 것 같아서 더 만들지 않음
- 판정 결과가 `해당 없음`인 항목은 만들지 않았다고 **명시적으로 보고**함. 조용히 건너뛰지 않음

### 3단계 — 되묻기 (답을 받기 전에는 코드를 만들지 않음)

아래를 기본값과 함께 물음. 사용자가 `기본값으로 진행`이라 하면 되묻지 않고 진행함.

| 물을 것 | 기본값 제시 | 왜 묻나 |
|--------|-----------|--------|
| 부분 전송 계약 — 이벤트 종류 · 보내는 순서 | 최종형만 1회 전송 | ③은 「프로세스별 API 설계」의 `응답 키`까지만 정함. 조각을 어떻게 나눠 보낼지는 칸이 없음 |
| 총 시간예산에 도달했을 때 잘린 답을 보낼까 끊을까 | 지금까지 만든 부분 + 잘렸다는 표시 | ③의 `초과 시 처리`는 착지 노드까지만 정함. 바깥으로 무엇을 내보낼지는 칸이 없음 |
| API 규격 문서를 코드에서 뽑을까 따로 쓸까 | 코드에서 뽑음 | `D-03`에 방식이 비어 있을 때만 물음 |
| **바깥에 여는 포트 번호와 프로세스 간 포트 번호** | 바깥 `8080` · 프로세스 간 `8081`부터 순번 | **설계 범위 밖**이라 어느 설계서에도 칸이 없음. 안 정하면 프로세스마다 번호가 겹침 |
| 프로토타입 파일의 경로 또는 파일명 | 기본값 없음 — **반드시 받음** | 2단계에서 화면을 만들기로 판정됐는데 경로가 없으면 9단계를 시작할 근거가 없음 |

- 2단계에서 `부분 전송 불필요`로 판정됐으면 첫 두 항목을 묻지 않음
- 2단계에서 `화면을 만들지 않음`으로 판정됐으면 프로토타입 파일 항목을 묻지 않음

### 4단계 — 요청 · 응답 모양을 정함

- 응답 본문은 `06`의 구조화 출력 스키마를 **그대로** 씀. 필드를 더하거나 이름을 바꾸지 않음
  (그 원본은 ③ 「프로세스별 API 설계」의 `응답 키`·「State 구조」임)
- `06-workflow.md`가 만든 **구조화 출력 스키마와 형 정의 1벌을 공유함.**
  같은 모양을 두 번 정의하면 한쪽만 고쳐져 어긋남
- 요청 본문은 ③ 「프로세스별 API 설계」의 `요청 키`와 「단계별 설계」의 **첫 단계가 받는 값**에서 나옴.
  없는 값을 받지 않음
- 오류 응답 모양을 **1벌로** 정함 — 어느 진입점이든 같은 모양으로 냄
- 오류 응답에 **안쪽 사정을 담지 않음** — 스택 · 쿼리 · 파일 경로 · 모델 이름을 내보내지 않음

### 5단계 — 진입 API를 만듦

- 2단계에서 센 진입점 수만큼 만듦. 그보다 많이 만들지 않음
- 각 진입점은 `06-workflow.md`의 **그 워크플로우 진입 함수**를 **부르기만** 함.
  흐름 판단을 여기서 하지 않음
- 요청이 들어오는 자리에 `05-guardrail.md`의 **입력 검사 훅**을 끼움
- 응답이 나가는 자리에 `05-guardrail.md`의 **출력 검사 훅과 가리기 훅**을 끼움.
  **가리기를 건너뛰는 경로가 하나도 없게** 함
- `01-runtime.md`의 마감선 필드에 값을 넣는 곳은 **여기 한 곳뿐임.** 안쪽 노드는 읽기만 함
- `스케줄 배치` 트리거 워크플로우는 API로 열지 않음. **실행기로 만들고 바깥에 포트를 안 엶**.
  `이벤트` 트리거는 구독자로 만듦
- 사전 승인 지점이 있으면 재진입 API를 1개 만듦.
  세션 식별자로 멈춘 자리를 찾아 이어서 돌게 함(`01-runtime.md`의 조립 규칙을 씀)

### 6단계 — 상태 확인 경로를 만듦

- `08-deploy.md`가 쓸 상태 확인 경로를 만듦. 경로 이름은 `D-03` 관례를 따름
- **뜰 준비가 됐는지**와 **살아 있는지**를 나눔.
  뜰 준비 확인은 필수 설정과 바깥 연결을 확인하고, 살아 있는지 확인은 가볍게 응답만 함
- 상태 확인 응답에 안쪽 사정을 담지 않음. 버전 · 설정값 · 연결 문자열을 내보내지 않음

### 7단계 — 부분 전송 경로를 만듦

- 2단계에서 `부분 전송 불필요`로 판정됐으면 **이 단계를 건너뜀.** 안 만들었다고 보고함
- 3단계에서 받은 계약대로 만듦 — 이벤트 종류와 보내는 순서를 지킴
- **조각에도 출력 검사와 가리기를 적용함.** 최종형에만 걸면 조각으로 원문이 새어 나감
- 중간에 끊겼을 때 무엇이 남는지 정함. 반쪽 답이 저장되지 않게 함
- 총 시간예산에 도달하면 3단계에서 받은 대로 처리함

### 8단계 — 포트와 설정을 뺌

- **포트 번호는 설계 범위 밖임**: 3단계에서 되물어 정한 값을 쓰고, **환경변수로** 읽게 함.
  코드에 숫자를 박지 않음
- 환경변수 이름은 `01-runtime.md`가 정한 `D-12` 규격을 따름
- 되묻기 답이 없으면 `[확인필요]`로 두고 기본값을 설정 파일에만 적음
- **바깥에 여는 것은 2단계가 센 진입점뿐임**(③ 「프로세스별 API 설계」가 노출한 엔드포인트).
  프로세스 사이 통신용 포트를 바깥에 열지 않음

### 9단계 — 화면을 만듦

- 2단계에서 `D-04`가 `해당 없음`이면 **이 단계를 통째로 건너뜀.** 안 만들었다고 보고함
- **프로토타입 파일이 없으면 이 단계를 시작하지 않음.** 3단계에서 경로 또는 파일명을 먼저 받음
- 프로토타입 파일을 읽어 **화면 요소 목록**(요소 이름 · 역할 · 상태 유무 · 대응 컴포넌트명 ·
  프로토타입 내 위치)을 먼저 만들어 응답에 보인 뒤 코드를 만듦
- 컴포넌트 경계는 프로토타입 구조를 따름. 프로토타입에 없는 요소를 **지어내지 않음**.
  꼭 필요한 요소(빈 상태 · 오류 · 로딩)가 프로토타입에 없으면 `프로토타입 미포함`이라 적고 최소 형태로 추가함
- `06`의 구조화 출력 스키마의 값을 보여 주는 화면만 만듦. 설계서에 없는 화면을 만들지 않음
- API 응답 형을 프론트에서 **다시 정의하지 않음.** 한 곳에서 뽑아 씀
- 부분 전송을 쓰면 조각이 올 때마다 화면이 갱신되게 함
- 오류 응답을 사람이 읽을 수 있는 말로 바꿔 보임. 안쪽 사정을 화면에 띄우지 않음
- 화면 코드는 `D-05`의 프론트 위치에 둠. 백엔드 코드와 섞지 않음

### 10단계 — 시험을 만듦

- 단위 시험 도구는 `D-07`을 따름. 흐름과 바깥 연결은 전부 대역으로 바꿈
- 실제로 부르는 시험은 `D-07` 표식으로 분리함
- 반드시 넣을 시험 7종

| # | 무엇을 확인하나 |
|:-:|---------------|
| 1 | 응답 본문 필드가 `06`의 구조화 출력 스키마와 **같음**. 더 있는 필드 0건 |
| 2 | 가리기를 안 거치는 응답 경로가 **0건**(부분 전송 조각 포함) |
| 3 | 오류 응답에 스택 · 쿼리 · 파일 경로가 **0건** |
| 4 | 상태 확인 응답에 설정값 · 연결 문자열이 **0건** |
| 5 | 총 시간예산에 도달하면 3단계에서 정한 대로 처리함 |
| 6 | ③에 없는 진입점이 **0건**(경로 목록과 ③ 워크플로우 수가 같음) |
| 7 | 코드에 박힌 포트 번호 **0건** |
| 8 | `스케줄 배치`·`이벤트` 트리거 워크플로우가 바깥 포트로 열리지 **않음** |

### 11단계 — README를 만듦

- 개요 — 어떤 진입점이 있고 각각 무엇을 하나
- 가상환경 만들기와 실행 — **Windows GitBash · Windows PowerShell · Linux/Mac 3종**을 각각 적음
- 화면이 있으면 **프론트 실행 명령과 패키지 매니저를 따로** 적음(백엔드와 섞지 않음)
- 진입점 표 — `경로 / 하는 일 / ③의 어느 워크플로우·트리거에서 왔나 / 부분 전송 여부`
- 응답 · 오류 모양 예시
- 포트 표 — `이름 / 환경변수 / 바깥에 여나 / 어디서 정했나`(전부 되묻기 값임)
- **안 만든 것 목록** — 2단계 판정으로 건너뛴 것을 이유와 함께 적음
- 되묻기로 정한 값 목록 — 무엇을 무엇으로 정했고 설계서 어디에 되돌려 적어야 하는지

### 12단계 — 제출 전 자가 점검

아래를 스스로 판정해 `항목 / 판정(통과·미통과) / 근거` 3열 표로 응답에 제시함.  
미통과가 1건이라도 있으면 **완료라고 보고하지 않고** 고친 뒤 다시 판정함.

- [ ] 응답 필드가 `06`의 구조화 출력 스키마와 같음. 더 있는 필드 **0건**
- [ ] 진입점 수와 ③ 「워크플로우 표」의 행 수가 **같음**(트리거에 맞는 형태로 열었음)
- [ ] 가리기를 안 거치는 응답 경로 **0건**(조각 포함)
- [ ] 코드에 박힌 포트 번호 **0건**
- [ ] 오류 · 상태 확인 응답에 안쪽 사정 **0건**
- [ ] 형 정의를 두 번 만든 곳 **0건**(06과 공유, 프론트와 공유)
- [ ] 2단계에서 `해당 없음`으로 판정한 것을 만들지 **않았음**
- [ ] 단위 시험 실패 **0건**
- [ ] `[확인필요]` 건수를 숫자로 적었고 README 목록의 행 수와 같음

## [출력]

| 산출물 | 경로 |
|--------|------|
| 진입 API · 부분 전송 · 상태 확인 | `D-05`의 서비스별 모듈 위치 |
| 화면(`D-04`가 `해당 없음`이면 안 만듦) | `D-05`의 프론트 위치 |
| 단위 시험 | `D-05`의 시험 코드 위치 |
| 의존성 파일 | `D-06`이 정한 파일명. 프론트가 있으면 따로 1건 더 |
| README | 서비스별 모듈 위치 바로 아래 |

- 파일 이름과 디렉터리 이름은 `D-05` 규칙을 따름. 설계서 ② 「내부 관계도」 노드 이름과 맞춤
- 톤앤매너: 이 API를 처음 부르는 다른 팀 개발자가 **README만 읽고 요청을 보낼 수 있는** 수준으로 씀

## [제약조건]

- MUST
  - 응답 본문은 `06`의 구조화 출력 스키마를 그대로 씀. 형 정의는 `06-workflow.md`와 **1벌을 공유**함
  - 진입점 수는 ③ 「워크플로우 표」의 행 수를 넘지 않고, **트리거에 맞는 형태**로 엶
  - 모든 응답 경로에 `05-guardrail.md`의 출력 검사와 가리기를 끼움. **부분 전송 조각도 포함**
  - 포트 번호는 **3단계 되묻기로만** 정하고 환경변수로 뺌. 환경변수 이름은 `D-12` 규격을 따름
  - 마감선(총 시간예산) 값을 넣는 곳은 진입 API 한 곳뿐임
  - context7 MCP로 프레임워크 API를 **코드 작성 직전에** 확인함
  - 2단계에서 `해당 없음`으로 판정해 안 만든 것을 **명시적으로 보고**함
  - 값이 없으면 `[확인필요: 무엇]`으로 남기고 README 목록에 1행씩 적음
  - 화면을 만들 때는 **프로토타입 파일 경로 또는 파일명을 반드시 입력받고** 그 구조를 근거로 만듦
  - 추가 정보나 결정이 필요하면 **사용자에게 반드시 문의**함
- MUST NOT
  - 산출물 본문에 dash(`—`)를 쓰지 않음. 문장 연결은 콜론(`: `)이나 마침표로 함(표 셀의 `값 없음` 표시용 단독 `—`은 예외)
  - `06`의 구조화 출력 스키마에 없는 필드를 응답에 **더하지 않음**
  - ③에 없는 진입점 · 화면을 만들지 않음. 있으면 좋을 것 같아서 더 만들지 않음
  - 프로토타입 파일 없이 화면 구조 · 요소를 **임의로 지어내지 않음**
  - 오류 · 상태 확인 응답에 스택 · 쿼리 · 파일 경로 · 설정값 · 모델 이름을 담지 않음
  - 포트 번호 · 응답 시간 목표를 코드에 숫자로 박지 않음
  - 프론트에서 응답 형을 **다시 정의하지 않음**
  - 흐름 판단 · 검사 규칙 · 이미지 정의를 여기서 만들지 않음(6 · 5 · 8 몫)
  - `스케줄 배치`·`이벤트` 트리거 워크플로우를 바깥 포트로 열지 않음
  - 설계서에 값이 없다고 흔한 기본값으로 채워 넣지 않음. 되물음
- 완료조건 — 확인한 증거를 함께 냄
  - ⓐ 만든 파일 목록을 실제 `ls` 결과로 첨부함
  - ⓑ 단위 시험 실행 로그를 첨부하고 **실패 0건**임
  - ⓒ 응답 필드 수와 `06`의 구조화 출력 스키마 필드 수가 같음을 숫자로 보임
  - ⓓ 진입점 수와 ③ 워크플로우 수가 같음을 숫자로 보임(트리거별 형태를 함께 적음)
  - ⓔ 코드에 박힌 포트 번호가 **0건**임을 검색 결과로 보임
  - ⓕ 가리기를 안 거치는 응답 경로가 **0건**임을 검색 결과로 보임
  - ⓖ 표본 요청 **3건 이상**의 요청→응답 로그를 첨부함
  - ⓗ 12단계 자가 점검 판정 표를 제시하고 **미통과 0건**임
  - ⓘ 확인하지 않은 것을 완료로 적지 않음(`AGENTS.md` 정직한 보고 규칙)

## [예시]

아래는 **형식만** 보여 줌. 값은 설계서에서 나온 것으로 전부 교체함.

**2단계 적용 판정 결과 형식**

```
| 판정 항목 | 읽은 값 | 출처 | 만드나 |
|----------|--------|------|:-----:|
| 화면 | 해당 없음 | D-04 | 안 만듦 |
| 부분 전송 | 필요 — 최악값 합계가 응답시간 목표를 넘음 | ③ 총 시간예산 · ① `목표값` | 만듦 |
| 워크플로우 수 | 3개(동기요청 1 · 스케줄 배치 1 · 이벤트 1) | ③ 「워크플로우 표」 | 진입 3종 |
| 동기요청 | 1개 | ③ `트리거 유형` | API 1개 |
| 스케줄 배치 | 1개 | ③ `트리거 유형` | 실행기로 만듦(포트 안 엶) |
| 이벤트 | 1개 | ③ `트리거 유형` | 구독자로 만듦(포트 안 엶) |
| 사전 승인 지점 | 있음 | ③ `인간 개입` 열 · 「재개 방안」 | 재진입 API 1개 |
| 바깥에 열 진입점 | 1개 | ③ 「프로세스별 API 설계」 · ⑦ 물리 배치도 | 포트 번호는 되묻기로 정함 |
```

**진입점 표 형식** (README에 넣음)

```
| 경로 | 하는 일 | ③의 어느 워크플로우 · 트리거 | 부분 전송 |
|------|--------|---------------------------|:--------:|
| POST /{자원명} | 흐름을 처음부터 돌림 | {워크플로우1} · 동기요청 | 예 |
| POST /{자원명}/{식별자}/resume | 멈춘 자리에서 이어 돌림 | {워크플로우1} · 사전 승인 뒤 | 아니오 |
```

**포트 표 형식**

```
| 이름 | 환경변수 | 바깥에 여나 | 어디서 정했나 |
|------|---------|:-----------:|------------|
| 진입 | {접두}_HTTP_PORT | 예 | 3단계 되묻기 |
| 프로세스 간 | {접두}_INTERNAL_PORT | 아니오 | 3단계 되묻기 |
```

**완료 보고 형식** (응답으로 냄. 코드에는 넣지 않음)

```
만든 파일: (ls 결과 첨부)
06 구조화 출력 스키마 필드 8개 → 응답 필드 8개 (같음. 더 있는 필드 0건)
③ 워크플로우 3개 → 진입 3종(API 1 · 실행기 1 · 구독자 1) (같음)
코드에 박힌 포트 번호 0건 (검색 결과 첨부)
가리기 안 거치는 응답 경로 0건 (검색 결과 첨부)
단위 시험 21건 실행 · 실패 0건 (로그 첨부)
표본 요청 3건 요청→응답 로그 (첨부)
안 만든 것 2건 — 화면(D-04 해당 없음) · 부분 전송 아님(2단계 판정)
되묻기로 정한 값 1건 — 마감선 도달 시 처리(③에 되돌려 적을 것)
[확인필요] 1건 — README 목록 1행과 일치

| 점검 항목 | 판정 | 근거 |
|----------|------|------|
| ...(12단계 전건) | | |
```

**해서는 안 되는 것 (anti-example)**

```
목록 조회 API와 관리자 화면도 있으면 편할 것 같아 같이 만들었음.
응답에 디버깅용으로 안쪽 처리 시간과 모델 이름 필드를 더했음.
부분 전송 조각은 최종형에서 이미 검사하므로 검사를 건너뛰었음.
```

셋 다 틀림. 설계서에 없는 것은 **만들지 않고**, 응답 필드는 **더하지 않으며**,
가리기와 검사는 **조각에도 걸어야 함**. 조각으로 나가는 원문은 최종형 검사로 못 막음.
