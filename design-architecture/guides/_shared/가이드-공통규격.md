# 설계 가이드 공통 규격 v1

토론: 클로니·플로니·지식니·커넥니 · 확정: 클로니 · 2026-08-06

`prompts/design/`의 설계 산출물 7종 작성 프롬프트를 **사람이 읽고 스스로 설계할 수 있는 가이드**로
옮기기 위한 규격임. 가이드 7건은 `guides/` 아래에 두며 이 규격을 공유함.

---

## 0. 프롬프트와 가이드는 무엇이 다른가

| 구분 | 프롬프트 (`prompts/design/`) | **가이드 (`guides/`)** |
|------|-----------------------------------|----------------------|
| 받는 쪽 | AI 코딩 도구 | **사람** (KT 3일 과정 팀 · 신한카드 10주 조) |
| 형식 | `{변수}` 치환 템플릿 · MUST / MUST NOT / 완료조건 | 판단 순서 · 판정표 · 점검표 |
| 순서 | 규격을 빠짐없이 훑는 순서 | **사람이 실제로 판단하는 순서** |
| 목적 | 빠짐없이 생성 | **스스로 판단** |

**가이드는 프롬프트의 해설이 아님.** 아래 두 가지를 반드시 함.

1. **판단 순서를 정함** — ⑤ 지식/도구는 `질문 유형 뽑기 → 경로 판정 → 버린 대안 기록` 순으로 판단함.
   프롬프트는 이 순서를 가이드에서 받아 씀(2026-08-08 프롬프트 재작성으로 두 문서가 같은 순서가 됨)
2. **프롬프트에 없는 칸을 메움** — 토론에서 3명이 각각 프롬프트에 없는 필수 항목을 지목함(4절 참조).
   그것을 채우지 않으면 가이드가 프롬프트 재탕이 됨

---

## 1. 섹션 구조 (5개 고정)

제목 문자열을 **글자 그대로** 사용함.

| 순서 | 섹션 제목 | 무엇을 쓰는가 | 분량 |
|------|----------|--------------|------|
| 0 | `# 설계서 {번호} {산출물명} — 작성 가이드` | H1 1줄 | 1줄 |
| 1 | `## 1. 이 설계서가 답하는 질문` | 한 줄 정의 · **이걸 안 하면 나는 사고 3줄** · 앞뒤 연결도(Mermaid) | 20줄 |
| 2 | `## 2. 시작 전 판정` | 5분 안에 끝나는 결정 트리 또는 판정표. 여기서 막히면 뒤가 다 막힘 | 35줄 |
| 3 | `## 3. 설계항목 작성법` | 4열 표 — `설계항목 / 무엇을 적나 / 어디서 가져오나 / 예시`. `무엇을 적나`는 **규격 한 줄 + 대시 불렛 2 ~ 5개**로 씀(문장 뭉치 금지). 규격에 빠져 있어 설계자가 직접 메워야 하는 항목도 이 절에 함께 둠 | 30 ~ 80줄 |
| 4 | `## 4. 제출 전 자가 점검표` | 프롬프트 완료조건을 사람이 확인 가능한 문장으로 옮김. 체크박스 | 20줄 |
| 5 | `## 5. 다음으로 넘기는 값과 근거 자료` | 후행 소비처 표 + 근거 아티클 표 | 20줄 |

**`무엇을 적나`의 규격 한 줄** — 셀의 첫 줄에 두고 그 뒤에 기존 대시 불렛을 이어 씀.
형식은 아래로 고정하며 **해당하는 요소만** 씀(불렛으로 세지 않음).

```
**형태** {값의 모양} · **비면** {빈칸에 적을 값} · **주인** {값의 소유 산출물} · **대조** {채운 뒤 맞출 것}
```

| 요소 | 무엇을 적나 |
|------|-----------|
| `형태` | 그 칸에 들어갈 값의 모양 — `숫자 1개 + 단위` · `2값 중 1개` · `S-n 식별자` · `표.열 꼴` 같이 |
| `비면` | 값이 없을 때 적는 것 1개 — `[확인필요: 무엇]` · `해당 없음` · `0건` 중에서 고름 |
| `주인` | 값의 주인이 **다른 산출물일 때만** 씀. 본 산출물이 주인이면 이 요소를 생략함 |
| `대조` | 다 채운 뒤 무엇과 개수·문자열을 맞추는지 |

**총 분량 — 의미를 이해할 수 있게 상한을 없앰**
근거: 3일 과정에서 **20분 넘게 걸리는 문서는 읽히지 않음**(커넥니).
대신 README에 **최소 경로**(2절·3절·4절만, 1건 약 7분)를 둠.

**2026-08-08 구조 변경** — 사용자 결정으로 종전 `4. 예시`와 `5. 자주 틀리는 5가지`를 **삭제**하고
절 번호를 다시 매겼음(종전 6절 → 4절, 7절 → 5절). 3절 표에서 `나쁜 예` 열을 지우고
`좋은 예`를 `예시`로 바꿨으며, 빠진 판단 유형을 `무엇을 적나`의 규격 한 줄로 메꿨음.
삭제한 두 절의 내용은 git 이력에 있음.

**2026-08-07 구조 변경** — 종전 `4. 프롬프트에 없지만 반드시 채울 것`을 **별도 절로 두지 않고 3절에 합쳤음.**
꼭 채워야 하는 설계항목이면 3절 표의 행이거나 그 표 아래 보충 설명이어야 하며, 따로 떼면
설계자가 두 곳을 왕복하게 됨. 3절 제목도 `칸별 작성법` → **`설계항목 작성법`** 으로 바꿨음.

---

## 2. 쉬운 언어 규칙

`CLAUDE.md` Lessons Learned의 `[Highly Important] 쉬운 언어 사용`을 이 문서에 맞게 편 것임.

- **G1** 한 문장 60자 이내를 원칙으로 함. 60자 초과가 전체의 10%를 넘지 않음(표·코드 제외)
- **G2** 영문 전문용어는 처음 나올 때 그 자리에서 한 줄로 풀어씀. 별도 용어표는 두지 않음
  (가이드는 참조 문서가 아니라 작업 중에 읽는 문서이므로 뒤로 넘기지 않음)
- **G3** 프롬프트의 규격 용어(`HARD`·`CYCLE`·`T-1`·`C-2` 등)를 쓸 때는 **뜻을 먼저 한 줄로 풀고** 씀
- **G4** "적절히"·"충분히"·"잘" 금지. 판단 기준은 숫자나 예/아니오 질문으로 씀
- **G5** 지시가 아니라 **설명**으로 씀. `~하시오`가 아니라 `~함` / `~이 낫습니다`
- **G6** 표를 우선함. 3개 이상 나열은 표로 바꿈
- **G7** 한 줄 120자 제약은 **산문과 목록에만 적용함.** 표 행은 줄바꿈이 안 되므로 제외함.
  실제 작성에서 4명이 같은 지적을 했음(④③⑥⑦)

---

## 3. 판정 확정 사항 (토론 결과)

종전 산출물 규격 6·7절의 미결 3건을 토론으로 확정함(그 규격은 `guides/README.md`로 옮겼음). 가이드에 이 판정을 반영함.

| ID | 미결 내용 | 확정 | 근거 |
|----|----------|------|------|
| G-4 | 타임아웃을 ③ 패턴/시퀀스·⑥ 가드레일/관측 중 누가 소유하나 | **③ 패턴/시퀀스 소유 · ⑥ 가드레일/관측 참조** (규격 유지) | 예산을 나눠 배정하는 주체가 값을 가짐. **단 ⑥ 가드레일/관측에서 값이 안 맞으면 ⑥ 가드레일/관측을 고치지 말고 ③ 패턴/시퀀스에 `변경요청` 1행을 남기는 경로를 가이드에 명시**(커넥니) |
| G-5 | 실패 대응 통합 확인 기준을 ④ 역할계약서·③ 패턴/시퀀스 중 어디에 두나 | **③ 패턴/시퀀스로 확정** | 중단 조건(④ 역할계약서)은 담당자 1명의 판단이나 재시도·폴백·타임아웃은 시퀀스 전체의 대응임. 통합 확인은 시퀀스 소유자에서만 성립함(커넥니) |
| G-10 | Mock 구분 목록을 ⑤ 지식/도구·② 논리아키텍처 중 어디에 두나 | **⑤ 지식/도구 유지 · 값 소유는 ② 논리아키텍처로 선언** | G-4와 같은 방식이라 규격이 일관됨. 목록째 ② 논리아키텍처로 옮기면 커넥터 입출력 규격과 분리돼 C-2에서 다시 어긋남(지식니) |

### 3-2. 런치픽 설계 테스트로 추가 확정한 3건 (2026-08-06)

가이드 7종을 실제 기획 산출물에 적용해 설계서 7종을 만드는 과정에서 **소유자가 없어 충돌이 실제로 발생한** 항목임.
근거는 [`design/00-반영대조표.md`](../../design/00-반영대조표.md) 6절 `J-1` ~ `J-9`임.

| ID | 미결 | 확정 | 근거 |
|----|------|------|------|
| G-11 | 민감 필드 ID(`F-n`)를 ④ 역할계약서·⑤ 지식/도구 중 누가 소유하나 | **⑤ 지식/도구 소유 · ④ 역할계약서는 인용만.** ④ 역할계약서에만 있는 필드는 ⑤ 지식/도구가 뒤에 번호를 더하며 기존 번호를 재부여하지 않음 | ⑤ 지식/도구가 민감 필드 분류표를 필수 출력으로 가짐. 실제로 `④ 역할계약서 F-2 = 식이 유형` / `⑤ 지식/도구 F-2 = 정확 좌표`로 갈려 ③ 패턴/시퀀스·⑥ 가드레일/관측이 인용할 표를 특정할 수 없었음 |
| G-12 | 입출력 **키 이름**을 ④ 역할계약서·⑤ 지식/도구 중 누가 소유하나 | **④ 역할계약서 소유 · ⑤ 지식/도구 커넥터 규격이 맞춤.** ③ 패턴/시퀀스는 `키 집합 식별자`까지만 소유하고 이름은 붙이지 않음 | ④ 역할계약서에 `키 이름의 단일 출처` 선언이 이미 있음. ③ 패턴/시퀀스가 ④ 역할계약서보다 먼저 쓰이므로 이름을 가질 수 없고 흐름의 단위만 확정 가능함 |
| G-13 | 커넥터의 범위 | **외부 시스템 접근만 커넥터임.** 내부 저장소는 `커넥터 아님`으로 구분해 적음 | 내부 저장소는 ⑤ 지식/도구 정형 접근 경로 표의 대상임. 구분이 없어 ④ 역할계약서가 저장소 접근 수단 이름을 ⑤ 지식/도구에 되물었음 |

`G-11`과 `G-12`는 **서로 반대 방향**임 — 필드 ID는 ⑤ 지식/도구가, 키 이름은 ④ 역할계약서가 소유함. 근거가 각각 다름.

---

## 4. 프롬프트에 없어 가이드가 메워야 하는 항목

토론에서 3명이 지목한 것임. 해당 가이드의 3절에 반드시 넣음.

| 가이드 | 빠진 항목 | 왜 필요한가 |
|--------|----------|------------|
| ④ 역할계약서 | **단일·멀티 판정 사다리** | 프롬프트는 "3줄 판정 근거"만 요구함. 결론 칸일 뿐 판단법이 없음 |
| ③ 패턴/시퀀스 | **상태 필드 소유권 규칙** | 병렬 분기에서 두 노드가 같은 필드를 쓰면 덮어쓰기로 값이 사라짐. `필드당 갱신 주체 1개, 병렬이면 누적만` |
| ③ 패턴/시퀀스 | **반복 상한(`max_iter`)과 소진 시 착지 노드** | 표에 칸 자체가 없음. "실패 시 재계획"이 무한 왕복이 됨 |
| ③ 패턴/시퀀스 | **재시도 포함 최악값 합계** | 타임아웃 합계 검증이 성공 경로만 더해 예산을 통과시킴 |
| ⑤ 지식/도구 | **원천 데이터 오류율 측정 칸** | KT 기획서의 정합성 오류 25 ~ 30%가 그대로 검색 실패율이 됨 |
| ⑤ 지식/도구 | **골든셋 20 ~ 30문항 만드는 법** | 평가 없이 청킹·모델부터 손대는 순서 오류를 막음 |
| ⑥ 가드레일/관측 | **관측 기록을 마스킹 대상 표의 행으로** | 출력 직전 1회만 가리면 관측 기록·오류 로그에 원문이 남음 |
| ⑦ 배포 | **비밀값 위반 예시 3종** | 이미지 레이어 · 매니페스트 평문 · 로그 출력. 금지만 적혀 있고 무엇이 위반인지 안 보임 |

### 4-2. 실제 설계에서 추가로 드러난 공백 (2026-08-06)

런치픽 설계 테스트에서 **가이드를 그대로 따르면 계산이 아무도 안 되거나 가드레일이 사라지는** 자리임.
전체 86건 중 성격이 다른 3종만 여기 올림. 나머지는
[`guides/00-검증결과-반영내역.md`](../00-검증결과-반영내역.md)에 있음.

| 성격 | 무엇이 빠졌나 | 왜 필요한가 |
|------|-------------|------------|
| **입력 전제 승계** | 가이드가 프로젝트 파라미터 파일(당시 `00-파라미터블록.md` · 현재 `prompts/design/_shared/프로젝트정보.md`)을 한 번도 언급하지 않음 | ⑦ 배포의 비밀값 도출이 `기술 스택 6행`에 걸려 있고, 그 표는 **② 논리아키텍처의 부속 표가 아니라 파라미터 `V-07`**임. ② 논리아키텍처에는 없고 있을 수도 없어 ⑦ 배포가 시작 지점에서 막힘 |
| **곱셈·나눗셈 담당** | 값의 **소유자**는 정했으나 두 문서의 값을 **곱하거나 나누는 계산**의 담당자가 없음 | `총량 ÷ 단계 수`(① 목표/품질 카드 ÷ ③ 패턴/시퀀스) · `동시 사용자 × 자원 점유 비율`(① 목표/품질 카드 × ⑦ 배포) · `기간 상한 ÷ 건수 전제`(① 목표/품질 카드 ÷ 기획)를 아무도 하지 않았음. 교차검증 HIGH 8건 중 5건이 이 자리 |
| **칸 사이 모순 검사** | 4절 점검표가 **칸의 존재·개수만** 봄 | ④ 역할계약서에서 `사용 모델`에 모델이 있는데 `사용 도구`에 그 모델을 부르는 도구가 없는 모순이 3판까지 10개 항목을 모두 통과했음 |

**7종 공통으로 넣을 점검 항목 2종**임. 각 가이드 4절에 형식을 맞춰 넣음.

| # | 항목 | 왜 |
|---|------|-----|
| 공통-1 | `칸과 칸이 서로 어긋난 행이 0건임` — 무엇과 무엇을 대조하는지 산출물마다 지정함 | 위 3번째 공백 |
| 공통-2 | 값이 필수인 칸은 `값이 있거나 [확인필요]로 되물어진 상태임(빈 칸 0건)` | ③⑤⑥⑦이 각각 **`공란 0건` 요구와 `추정값 금지` 요구가 부딪히는 것**을 기록했음. 원문에 숫자가 없으면 둘 중 하나를 어겨야 했음 |

---

## 5. 근거 아티클 연결 규칙

`references/articles/` 정리본 11종을 가이드 5절 `근거 자료` 표에 연결함.
**아티클을 요약해 옮기지 않음.** ID와 어느 칸에 거는지만 적고 링크로 넘김.

| 형식 | 예 |
|------|-----|
| `[A06](../../references/articles/A06_std_owasp-agentic-top10.md) §7` | `⑥ 가드레일/관측 표의 위협 열 고정 선택지(ASI01 ~ ASI10)` |

토론에서 확정한 매핑임.

| 아티클 | 거는 가이드·칸 |
|--------|--------------|
| A01 Anthropic 단일·멀티 | ④ 역할계약서 판정 근거 칸 — 컨텍스트 오염·병렬화·전문화 3분기 체크리스트. **단일이 기본값** |
| A02 LangChain Multi-agent | ③ 패턴/시퀀스 후보 표 — Subagents·Handoffs·Skills·Router·Custom workflow 5종 후보 풀 |
| A03 컨텍스트 엔지니어링 | ③ 패턴/시퀀스 상태 스키마 — `Runtime Context`·`State`·`Store` 3출처로 필드 위치 판정 |
| A04 MCP 명세 | ⑦ 배포 저장소 배치 — 무상태화로 세션 핸들이 도구 인자가 되므로 `핸들 보관 위치` 1행 |
| A05 AgentArcEval | ① 목표/품질 카드의 양식(사례연구 카드는 **9칸**) · ④ 역할계약서 중단 조건 — 실패 시나리오 카탈로그 |
| A06 OWASP | ⑥ 가드레일/관측 위협 열 고정 선택지 · ④ 역할계약서 최소권한 · ⑤ 지식/도구 정형 접근 금지 컬럼 |
| A07 OTel | ⑥ 가드레일/관측 기록 지점 스팬 이름. **`Development`(표준 후보) 병기 필수** |
| A08 Ragas 도구 지표 | ⑤ 지식/도구 커넥터 검증 기준 — `ToolCallAccuracy`는 **순서가 틀리면 인자가 다 맞아도 0점** |
| A09 Ragas Text-to-SQL | ⑤ 지식/도구 NL2SQL 평가 — 실행 결과 대조. **BookSQL은 비상업 라이선스라 자기 도메인 30문항으로 대체** |
| A10 금융위 가이드라인 | ① 목표/품질 카드 성격 선언 · ⑥ 가드레일/관측 출력측 검사. **7-3절 `최소수집`·`비식별`은 이 자료에 근거 없음** |
| B01 Essential GraphRAG | ⑤ 지식/도구 비정형 경로 선택 근거 · 청킹 기본값 · GraphRAG 비용 트레이드오프 |

**주의** — A10의 `최소수집`·`비식별` 2칸은 금융위 가이드라인에 근거가 없음.
가이드에 "다른 법령 근거가 필요하며, 못 찾으면 `[확인필요]`로 남김"을 명시함.
근거 없이 채우면 규제 문서 오인용이 됨.

---

## 6. 3절 `예시` 열 작성 규칙

**한 칸에 한 값만 적음.** 종전 `좋은 예`·`나쁜 예` 2열을 `예시` 1열로 합쳤으므로
칸에 남길 것은 **그 칸에 실제로 들어가도 되는 값 1개**임.

| 규칙 | 내용 |
|------|------|
| 길이 | 30자 이내. 설명이 필요하면 그것은 `무엇을 적나`의 몫임 |
| 형태 | `무엇을 적나`의 `형태` 요소와 **모양이 같아야 함**(숫자면 숫자, 식별자면 식별자) |
| 도메인 | 특정 프로젝트 값이 아니라 **어느 과제에서도 읽히는 값**으로 씀 |
| 금지 | 하지 말아야 할 값을 여기 적지 않음. 그것은 `무엇을 적나`의 `비면` 요소로 대신함 |

**예시가 정답이 아님** — 팀이 다르게 판단할 수 있고, 근거를 남기면 됨.

---

## 7. 파일명

```
guides/{번호}-{산출물명}-가이드.md
```

| 번호 | 파일명 | 담당 |
|------|--------|------|
| ① 목표/품질 카드 | `01-목표품질속성카드-가이드.md` | 클로니 |
| ② 논리아키텍처 | `02-논리아키텍처-가이드.md` | 클로니 |
| ④ 역할계약서 | `04-에이전트역할계약서-가이드.md` | 플로니 |
| ③ 패턴/시퀀스 | `03-패턴시퀀스설계-가이드.md` | 플로니 |
| ⑤ 지식/도구 | `05-지식도구설계-가이드.md` | 지식니 |
| ⑥ 가드레일/관측 | `06-가드레일관측설계-가이드.md` | 커넥니 |
| ⑦ 배포 | `07-배포설계-가이드.md` | 커넥니 |

`guides/README.md`에 7건 목록과 읽는 순서를 둠.

---

## 8. 완료조건

가이드 1건이 아래를 전부 만족해야 완료임.

- ⓐ 총 **120 ~ 260줄, 4,000 ~ 12,000자** — **글자 수는 개행 문자를 제외하고 셈**(통상 편집기 기준).
  **재는 방법을 아래로 고정함.** 눈으로 어림하지 않음
  ```bash
  # 개행을 LF로 고정한 뒤(.gitattributes 에 `*.md text eol=lf`) 아래를 돌림
  for f in guides/0*-*.md; do c=$(tr -d '\n\r' < "$f" | wc -m); l=$(wc -l < "$f"); \
    { [ "$c" -gt 12000 ] || [ "$c" -lt 4000 ] || [ "$l" -gt 260 ] || [ "$l" -lt 120 ]; } \
    && echo "규격 위반: $f (${l}줄 ${c}자)"; done
  ```
  **`\r`를 함께 지우는 것이 핵심임.** 윈도우 줄바꿈(`CRLF`)은 개행이 `\r\n` 2바이트인데
  `tr -d '\n'`만 쓰면 `\r`가 남아 **줄 수만큼 글자가 더 세어짐.** 2026-08-06 실제로 이 일이 났음 —
  잘못 재서 3종을 `초과`로 판정했고, 팀이 필요 없는 분량 축소를 했음. 다시 재니 전부 규격 안이었음.
  같은 파일이 재는 사람에 따라 다른 값이 나오면 판정이 갈리므로 **개행 방식을 먼저 고정함.**
  가이드를 고칠 때는 **넣는 만큼 줄임.** 규격을 깨고 넣지 않으며, 못 넣은 것은 미반영으로 기록함
  **줄 수 이력 — 180 ~ 360줄 → 120 ~ 260줄(2026-08-08).** 4절 예시와 5절 함정을 삭제해
  7건이 145 ~ 190줄로 내려갔음. 종전 하한 180줄을 그대로 두면 **간결화한 문서가 규격 위반이 됨.**
  줄이는 것이 목적이므로 하한을 낮춤. 상한은 읽는 시간(1건 20분)에 맞춰 260줄로 내림
  **글자 수 상한 이력 — 7,500 → 12,000자(2026-08-07).** 상한은 유지함.
  4절·5절 삭제로 7건이 6,000 ~ 10,500자가 되어 종전 초과 2건(③④)이 함께 해소됐음
  **분량의 진짜 상한은 글자 수가 아니라 260줄과 읽는 시간임** — 글자 수는 참고 지표로 둠
- ⓑ 섹션 제목 **5개**가 규격 문자열과 일치
- ⓒ 1절에 앞뒤 연결도(Mermaid) 1개와 "안 하면 나는 사고" 3줄이 있음
- ⓓ 2절에 결정 트리 또는 판정표가 있고, 판단 기준이 예/아니오 또는 숫자로 되어 있음
- ⓔ 3절 설계항목 표가 **4열**이고 `예시`가 **모든 행에** 있음
- ⓕ 3절 설계항목 표의 `무엇을 적나`가 **모든 행에 규격 한 줄 + 대시 불렛 2 ~ 5개**를 가짐
- ⓖ 3절에 프롬프트에 없는 항목이 1개 이상 있음(본 문서 4절 표 참조)
- ⓗ 4절 점검표가 체크박스이고 각 항목이 존재·개수·공란·수치 4형식 중 하나로 확인 가능함
- ⓘ 5절에 후행 소비처 표와 근거 아티클 표가 각각 있음
- ⓙ 60자 초과 문장이 전체의 10% 이하
