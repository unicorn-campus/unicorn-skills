# 가이드 검증 결과와 반영 내역

> **작성 순서 주의** — 이 기록은 `역할계약서 → 지식/도구 → 패턴/시퀀스` 순서였을 때의 검증 결과임.
> 2026-08-06 판정으로 번호가 `③ 패턴/시퀀스` · `④ 역할계약서`로 바뀌었고 본문 기호는 새 번호로 고쳤음.

> **가이드 절 번호 주의** — 2026-08-07에 가이드 구조가 8절 → 7절로 바뀜(종전 `4. 프롬프트에 없지만
> 반드시 채울 것`을 3절에 합치고 3절 제목을 `설계항목 작성법`으로 바꿈). 아래 본문의 절 번호는
> **당시 번호**임. 대응은 `4절 → 3절` · `5절 → 4절` · `6절 → 5절` · `7절 → 6절` · `8절 → 7절`이며
> `5-1`·`5-2` 예시는 `4-1`·`4-2`임.

작성: 2026-08-06 · 통합: 클로니(오케스트레이터)  
검증 방식: 가이드 7종을 **실제 기획 산출물에 적용해 설계서 7종을 만들어 봄**  
대상 케이스: 직장인 점심 추천 앱 `런치픽` (`~/workspace/lunch-aws-actions/docs/plan/`)  
산출물: [`design/`](../design/) · 통합 기록: [`design/00-반영대조표.md`](../design/00-반영대조표.md)

---

## 1. 어떻게 검증했나

문서를 읽고 검토한 것이 아니라 **가이드대로 설계서를 만들어 보고, 막힌 자리를 기록**했음.

| 단계 | 누가 | 무엇을 |
|------|------|-------|
| 1 | 클로니 | ① 목표/품질 카드 → ② 논리아키텍처 |
| 2 | 플로니 · 지식니(병렬) | ④ 역할계약서 · ⑤ 지식/도구 |
| 3 | 클로니 | C-1 순환(④ 역할계약서↔⑤ 지식/도구) 중개 — 충돌 4건 조정 |
| 4 | 플로니 · 커넥니(병렬) | ③ 패턴/시퀀스 · ⑦ 배포 |
| 5 | 모달니 | **바깥에서 교차검증** — 지연·비용·규제 관점으로 숫자를 다시 셈 |
| 6 | 커넥니 | ⑥ 가드레일/관측 |
| 7 | 클로니 | 반영 대조표 · 통합 검수 · 가이드 수정 배분 |

각 산출물 말미에 `가이드 적용 기록`(막힌 곳 / 우회 방법 / 수정 제안)을 남기게 했음.
**"문제 없음"으로 끝내지 못하게** 하고, 반대로 없는 결함을 만들지도 못하게 했음.

---

## 2. 발견 건수와 반영 결과

| 출처 | 발견 | A 반영 | B·C 미반영 |
|------|-----:|-------:|----------:|
| ① 목표/품질 카드 | 7 | 7 | 0 |
| ② 논리아키텍처 | 5 | 5 | 0 |
| ④ 역할계약서 | 14 | 6 | 8 |
| ③ 패턴/시퀀스 | 11 | 9 | 2 |
| ⑤ 지식/도구 | 15 | 9 | 6 |
| ⑥ 가드레일/관측 | 12 | 9 | 3 |
| ⑦ 배포 | 12 | 8 | 4 |
| 교차검증(모달니) | 10 | 7 | 3 |
| **합계** | **86** | **60** | **26** |

중복을 합치면 서로 다른 항목 **약 55건**임. 미반영 26건의 사유는 5절에 있음.

---

## 3. 가장 무거운 발견 3건

4명이 **서로 다른 산출물에서 같은 방향을 지목한 것**임. 개별 가이드 수정으로는 못 막는 구조 문제임.

### 3-1. 가이드가 프롬프트의 입력 전제(파라미터 블록)를 승계하지 않음

종전 파라미터 블록 파일(현재 `prompts/design/_shared/프로젝트정보.md`)에 **런치픽용 사례 B가 이미 채워져 있었음.**
같은 기획 문서에서 도출된 것인데 `guides/` 7종은 그 파일을 **한 번도 언급하지 않았음.**

| 무엇이 일어났나 | 결과 |
|---------------|------|
| ⑦ 배포 담당이 비밀값 도출에서 막힘 | ⑦ 배포 가이드 2-3이 `기술 스택 표는 ② 논리아키텍처의 부속 표임`이라 했으나 **② 논리아키텍처에 그 표가 없고 있을 수도 없음.** 실제 출처는 파라미터 `V-07`임 |
| ⑥ 가드레일/관측 담당은 **몰랐기에 오히려 옳게 작성함** | 사례 B가 `출력 측 노출 검사 = 0건`이라 적었으나 **실제로는 해당했음.** 봤다면 출력측 검사 절을 통째로 지웠을 것임 |
| 반영 대조표 1종이 규격 미충족으로 끝남 | `{범위 제외 목록}`이 `프로젝트 확정 필요` 상태라 차단 위치를 적을 수 없었음 |

**반영** — README에 `시작 전에 반드시 채울 것 — 프로젝트 파라미터 12종` 절 신설(값을 그대로 믿지 말라는
주의 2건 포함) · ⑦ 배포 가이드 2-3의 사실 오류 정정과 미기입 시 대체 도출 절차 승격 ·
⑥ 가드레일/관측 가이드 4-2에 `계열 0건 표기는 그대로 믿지 않고 재판정함` 추가 · ② 논리아키텍처 가이드 8절에 소유 관계 1줄.

### 3-2. 값의 소유자는 정했으나 두 문서의 값을 곱하거나 나누는 계산의 담당자가 없음

① 목표/품질 카드는 총량을, ③ 패턴/시퀀스는 단계 배정을, ⑦ 배포는 자원 상한을, ⑥ 가드레일/관측은 비용 상한을 각각 소유하고 **각자 규격을 지켰음.**
그런데 아래 계산은 **어느 문서의 칸에도 없어 아무도 하지 않았음.**

| 안 된 계산 | 결과 |
|-----------|------|
| `총량 ÷ 단계 수`의 검증 결과가 ① 목표/품질 카드로 돌아옴 | ① 목표/품질 카드가 `p95 3초(피크 포함)`으로 닫았는데 실제로는 **피크에서 약 3.3배 부족**이었음 |
| `동시 사용자 수 × 요청당 자원 점유 비율 ≤ 자원 상한` | 커넥션 풀 100개를 ⑦ 배포가 인용만 하고 ③ 패턴/시퀀스는 몰랐음. 처리량을 아무도 세지 않았음 |
| `기간 상한 ÷ 건수 전제` | 월 300만 원이 **요청당 10원**임을 ⑥ 가드레일/관측까지 아무도 계산하지 않았음 |
| `1 + max_iter` = 요청당 모델 호출 배수 | 반복 상한이 곧 단가 배수인데 시간 칸에만 걸려 있었음 |

교차검증 `HIGH` 8건 중 **5건이 이 자리**에서 나왔음.

**반영** — ① 목표/품질 카드 3절에 `단위 환산`·`채점 수단`·`분해 회신` 3칸 신설, 8절에 `되돌려 받는 값` 구획 ·
③ 패턴/시퀀스 4절에 **5번째 추가 칸 `부하 조건`** 신설과 `p95 합계`/`최악값 합계` 2줄 분리 ·
⑥ 가드레일/관측 5-1 `C-6`에 분모 1 분기, `C-4`를 `재시도 배수 × 루프 배수`로 분리 · ⑥ 가드레일/관측 8절 `되돌려 보내는 값` 구획.

### 3-3. 자가 점검표가 칸의 존재·개수만 보고 칸 사이의 모순을 통과시킴

④ 역할계약서에서 `사용 모델`에 모델명이 있는데 `사용 도구`에 그 모델을 부르는 도구가 없는 모순이
**3판까지 10개 점검 항목을 모두 통과했음.** 그 모순은 ⑦ 배포(이미지에 모델 배정)과 ⑥ 가드레일/관측(없는 호출에 비용 상한)까지
번졌고, 지식니가 **다른 산출물을 읽다가** 찾았음.

**반영** — 공통규격 4-2절에 7종 공통 점검 항목 2종 확정 ·
④ 역할계약서 7절에 `사용 모델 ↔ 사용 도구` 정합과 `US 요구사항 대비 누락 키 0건` 추가 ·
③⑤⑥⑦ 점검표의 `공란 0건` 요구를 `값이 있거나 [확인필요]로 되물어진 상태`로 완화(추정값 금지 규칙과의 충돌 해소).

---

## 4. 파일별 반영 내역

### guides/README.md (클로니)

| 반영 |
|------|
| `시작 전에 반드시 채울 것 — 프로젝트 파라미터 12종` 절 신설. 값을 그대로 믿지 말라는 주의 2건(오분류 사례·계열 0건 사례) 포함 |
| 파일명 규칙 정정 — `설계서{번호}-{산출물명}.md`(불명확) → 프롬프트와 같은 `01-목표품질속성카드.md` ~ `07-배포설계.md` 7개 + `00-반영대조표.md` |
| `근거 출처` 열은 본문 표에만 두고 부속 표 4종은 두지 않음(프롬프트 규칙과의 충돌 해소) |
| 선행 설계서 인용 규칙 — 새 약칭을 만들지 않고 원문 ID + `설계판단` 괄호 병기 |
| `표 행은 120자 제약에서 제외`(4명 중복 지적) |
| 소유 관계 `G-11`(민감 필드 ID = ⑤ 지식/도구) · `G-12`(키 이름 = ④ 역할계약서) · `G-13`(커넥터 = 외부만) 3건 신설 |
| `실제 설계에 써 본 결과` 절 신설 — 산출물 규모·순환 종료·조정 판정 9건·못 막은 것 3가지 |

### guides/_shared/가이드-공통규격.md (클로니)

| 반영 |
|------|
| 3-2절 신설 — `G-11`·`G-12`·`G-13` 확정과 근거. `G-11`과 `G-12`가 서로 반대 방향임을 명시 |
| 4-2절 신설 — 실제 설계에서 드러난 공백 3종(입력 전제 승계 · 곱셈·나눗셈 담당 · 칸 사이 모순 검사) |
| 4-2절에 **7종 공통 점검 항목 2종** 확정 |
| 2절에 `G7` 추가 — 120자 제약은 산문·목록에만 적용 |
| 8절 ⓐ에 **재는 명령과 개행 고정 방법** 확정(5절 참조 — 이번에 실제로 오판이 났음) |

### guides/01-목표품질속성카드-가이드.md (클로니)

`단위 환산`·`채점 수단`·`분해 회신` 3칸 신설 · `T-4 탈락 절반 초과 시` 대응 1줄 ·
탈락 사유를 반박해 보는 가름 1줄 · 원문 값 2개 충돌 시 계층 가름 1줄 ·
비율 목표 표본 산식(`1 ÷ (1 − 목표비율)`) · 대체 근거 탐색 순서 · 8절 `되돌려 받는 값` 구획 ·
1절 연결도에 되돌림 간선 2개 · 7절 점검 2줄 추가 · 4절 (2)의 잘못된 출처 3건을 표로 정리.

### guides/02-논리아키텍처-가이드.md (클로니)

판정 2에 **4번째 질문**(넘기지 **않기로** 한 항목) 신설 — 알레르기를 코드로도 보내지 않는 판정이
3질문으로는 나오지 않았음 · 판정 3에 `실물이어도 쓰려는 필드를 주는지 확인` · Mock 근거가 인허가에서
나온다는 1줄 · 3절에 `경계 미통과 항목`·`내부 저장소 목록` 2행 신설 · 4절 (3)에 찾은 노드를
`신설` 행으로 남기는 규칙 · 4절 (4) 내부 저장소 목록 표 · 8절에 `기술 스택은 ② 논리아키텍처가 소유하지 않음` ·
7절 점검 3줄 추가.

### guides/04-에이전트역할계약서-가이드.md (플로니)

`모델 미사용(결정론적 실행)` 좋은 예 · `사용 모델` 칸에 호출 설정과 모델 문서 확인일(기억 금지) ·
커넥터 ↔ 내부 저장소 구분(`G-13`) · 7절에 `사용 모델 ↔ 사용 도구` 정합 항목 ·
7절에 `US 요구사항 대비 누락 키 0건` · 1절에 `G-11` 1줄.

### guides/03-패턴시퀀스설계-가이드.md (플로니)

**`p95 합계`와 `최악값 합계` 2줄 분리** — 가이드가 `p95` 목표를 최악값으로 검증하라고 지시하고 있었음 ·
**5번째 추가 칸 `부하 조건`** 신설 · `곱함(재시도)`과 `더함(폴백)` 판별 2줄 · `Q1` 문구 교정
(조건 분기가 있어도 결정론이면 `예`) · `참여 주체에 저장소도 포함됨` 1줄 · 병렬 필드는 갈라 두기가
1순위 · `max_iter`에 `반복 1회 비용 증분` 열 · G-4 경로에 트리거 추가 요청 · 7절 점검 2건 완화.

### guides/05-지식도구설계-가이드.md (지식니)

2절 판정표 첫 줄에 **`답이 조건에 맞는 목록·정렬인가 → 정형 조회(검색 아님)`** — 정형이 본체인 앱이
전부 `[확인필요]`로 떨어지던 자리 · 판정표에 **`되돌릴 수 없는 판정이면 결정론 단계로 분리하고
LLM 입력 규격에서 그 필드를 지움`** · 4절 (1)을 `원천이 있나 → 오류율을 쟀나` 2단으로 분리하고
`갱신 지연` 칸 추가 · 4절 (2)에 `로그가 없는 신규 서비스` 분기와 표본 산식 · 3절에 `커넥터 사용 주체` ·
`민감 필드 ID = ⑤ 지식/도구 소유`와 `키 이름 = ④ 역할계약서 소유`(반대 방향임을 명시) · 7절 점검 3건.

### guides/06-가드레일관측설계-가이드.md (커넥니)

**`Q2` 문구 교정** — `외부 글을 읽나` → `우리가 쓰지 않은 문자열을 읽나(외부 API 응답·캐시 적재값 포함)`.
자유 입력창이 없는 앱에서 입력측 가드레일이 통째로 사라지던 자리 · `C-6` 분모 1 분기 ·
`C-4`를 `재시도 배수 × 루프 배수`로 분리 · 4-2에 계열 0건 재판정 · 2-3에 4단계(승인 불가 시 제한 장치로
대체) · 4-3에 `③ 패턴/시퀀스가 나눈 계층 수 = 기록 항목 수` · 8절 `되돌려 보내는 값` 구획 · `C-1`에 단가 판본과
모델 설정 · 7절에 문항 수 ↔ 목표값 눈금 점검.

### guides/07-배포설계-가이드.md (커넥니)

**2-3의 사실 오류 정정** — `기술 스택 표는 ② 논리아키텍처의 부속 표임`을 지우고 실제 출처(`V-07`)를 적고,
미기입 시 대체 도출을 정식 절차로 승격 · 배포 형태 허용값 **4종 통일**(가이드 3종 ↔ 프롬프트 3종이
서로 달랐음) · 2-2에 5번째 질문(보존 기간 혼재) · 2-1 상한을 조건절로 감싸고 `배포 절차 1종 고정`·
`줄이는 순서 미리 적기` 2기법 · 4-2 MCP 미채택 처리 · 4-3을 2단으로 분리(코드는 돌아가는데
데이터가 안 돌아오는 경우) · 7절 점검 2건.

---

## 5. 이번 작업에서 클로니가 낸 실수 2건

숨기지 않고 적음. 둘 다 **중개자·검증자 역할의 실패**임.

| # | 실수 | 어떻게 드러났나 | 무엇을 고쳤나 |
|:-:|------|---------------|-------------|
| 1 | **판정 `J-6`을 전원에게 전달하지 않음** — 지식니·커넥니에게만 보내고 플로니에게 빠뜨림 | ③ 패턴/시퀀스 2판에 J-6이 반영되지 않았고 모달니가 교차검증 `D-3`에서 잡음 | ③ 패턴/시퀀스 3판에서 반영. 협업형은 조정자가 판정을 **전원에게** 보내지 않으면 판정이 있어도 반영되지 않음 |
| 2 | **분량 측정 방법이 틀림** — `tr -d '\n'`으로 재어 3종을 `초과`로 판정하고 팀에 축소를 지시함 | 커넥니가 원인을 찾음 — 파일이 **윈도우 줄바꿈(CRLF)**이라 `\r`가 1자로 세어져 **줄 수만큼 부풀려짐.** 다시 재니 7종 모두 처음부터 규격 안이었음 | `.gitattributes`에 `*.md text eol=lf` 추가 · 공통규격 8절 ⓐ에 `tr -d '\n\r'`와 개행 고정 명시. **필요 없는 축소를 시킨 것은 잘못이며 원인은 클로니임** |

2번 때문에 ①②⑤⑥ 네 파일에서 불필요한 축소가 있었음. 내용이 빠진 것은 아니고 문장이 압축된 것이며,
② 논리아키텍처의 예시 도식 1개는 되살렸음.

---

## 6. 반영하지 않고 남긴 26건

| 사유 | 건수 | 대표 항목 |
|------|-----:|----------|
| **7종 공통 규격 소관** — 개별 가이드에 넣으면 7곳으로 갈라짐 | 8 | 자가 점검표의 칸 사이 모순 검사 · 두 문서 값을 곱하는 계산의 담당자 · 부속 표 열 규칙 |
| **다른 산출물과 짝으로 고쳐야 함** | 6 | ⑤ 지식/도구 민감 필드 표의 `검사 방식`·`검사 기준` 2열(⑥ 가드레일/관측 완료조건이 검사하는 열) · 요청당 토큰 수 전제(⑤ 지식/도구 ↔ ⑥ 가드레일/관측) |
| **분량 상한** | 5 | ⑤ 지식/도구 `5-3` 정형 본체·벡터 0건 예시 · ⑦ 배포 3절 `보존 기간 혼재` 칸 · ⑥ 가드레일/관측 예산 항목별 비용 칸 |
| **설계서 쪽 수정이며 가이드 칸 신설이 불필요** | 4 | Q-2 채점용 원시 입력 보존 · 태그 분리 · 대조 1행 추가 |
| **가이드 밖(운영 문서) 소관** | 3 | 팀 역할 분담 · 합격 기준 · `[확인필요]` 회수 절차 |

**분량 상한 5건은 다시 볼 여지가 있음** — 5절 2번 실수로 상한 판정이 실제보다 엄했음.
현재 7종의 여유는 200 ~ 265자이므로 짧은 항목은 들어갈 수 있으나, 이번 작업에서는 넣지 않았음.

---

## 7. 아직 검증되지 않은 것

| 항목 | 상태 |
|------|------|
| 수강생 대상 검증 | **안 했음.** 이번 적용자는 가이드를 쓴 팀원 4명임 |
| **교차검증 없이도 결함이 잡히는가** | **모름.** 이번에는 바깥 검증자(모달니)를 두었고, 그 검증이 뒤집은 판정 3건은 **자가 점검표 10항목을 모두 통과한 상태로 살아 있었음.** 즉 지금 점검표만으로는 그 3건을 못 잡음 |
| 설계서의 구현 검증 | **안 했음.** 7종 전부 `미검증 설계` 표기임 |
| 남은 순환 | ③ 패턴/시퀀스 ↔ ⑥ 가드레일/관측 타임아웃 참조(C-3)는 변경요청 경로로 처리했으나 실제 구현에서 다시 볼 항목임 |
| 되묻지 못한 `[확인필요]` | 설계서 7종에 **서로 다른 항목 63종**(실측). 그중 **16종은 2개 이상의 산출물이 같은 것을 물었음** — 그것이 진짜 병목임. 통합 목록은 [`design/00-반영대조표.md`](../design/00-반영대조표.md) 5절 |
| 규격 미충족 1건 | 제외 범위 미포함 대조표 — 파라미터 `V-09`가 `프로젝트 확정 필요`라 차단 위치를 적을 수 없음 |

## 8. 다음에 먼저 할 일

이번 결과에서 남은 가장 큰 위험은 **자가 점검표가 통과 도장 노릇을 하는 것**임.
교차검증이 뒤집은 판정 3건이 점검표 10항목을 전부 통과한 채 살아 있었기 때문임.

그래서 교육 과정에 쓰기 전에 아래 1건을 먼저 확인하는 것이 낫습니다.

> **가이드를 처음 보는 학습자 2 ~ 3명이 교차검증 없이 설계서를 만들게 하고,
> `HIGH`급 결함이 스스로 잡히는지 봄.** 안 잡히면 **교차검증을 과정의 필수 단계로 넣어야 함** —
> 바깥에서 숫자를 다시 세는 사람을 두는 것을 선택 사항으로 두면 이번 3건이 그대로 통과함.

공통규격 4-2절에 넣은 공통 점검 항목 2종(`칸 사이 모순` · `값 또는 [확인필요]`)이 그 3건 중
1건(모델 ↔ 도구 모순)은 잡을 수 있게 되었으나, 나머지 2건(탈락 사유 오진 · 피크 예산 미검증)은
**여전히 사람이 다시 세야 잡힘.**
