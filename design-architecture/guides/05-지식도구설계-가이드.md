# 설계서 ⑤ 지식/도구 — 작성 가이드

## 1. 이 설계서가 답하는 질문

**"이 담당자는 무엇을 근거로 답하는가."**  
질문마다 답을 어디서 가져올지, 민감한 값을 어디서 가릴지 못 박는 문서임.

**이걸 안 하면 나는 사고 3줄**

- 집계 질문을 문서 검색에 던져 숫자가 매번 달라짐. 시연에서 무너짐
- 고객 이름·전화가 가려지지 않은 채 외부 모델로 나감. 되돌릴 수 없음
- ⑦ 배포가 저장소를 못 정해 배포 설계가 멈춤

**앞뒤 연결도**

```mermaid
flowchart LR
  P2["② 논리아키텍처"] -->|HARD 민감라벨(SL)| P5
  P3["③ 패턴/시퀀스"] <-->|C-2 경로↔규격| P5["⑤ 지식/도구"]
  P4["④ 역할계약서"] <-->|C-1 필드↔권한| P5
  P5 -->|HARD 저장소| P7["⑦ 배포"]
```

용어 — `HARD`는 앞이 없으면 못 채움. `C-1`·`C-2`는 서로 보며 한 번 되돌리는 관계임.  
③ 패턴/시퀀스·④ 역할계약서는 ⑤ 지식/도구보다 **먼저** 쓰므로 ⑤ 지식/도구는 두 산출물의 값을 받아 맞추는 쪽임 — 새 이름을 지어내지 않음.

---

## 2. 시작 전 판정

**순서를 뒤집음.** 스택을 먼저 늘어놓지 않고 `질문 3개 → 경로 판정 → 버린 대안 1줄` 순으로 함.

**1단계 — 질문 3개 적기(5분).** 사용자가 실제로 칠 문장 그대로 적음.  
유저스토리의 `[검증 요구사항]` 조회형 항목에서 베끼면 빠름.

**"3개"는 판정 기준 개수가 아니라 표본 질문 개수임.** 아래 표는 질문 1개마다 처음부터 끝까지 통과시켜 경로를 정함.  
질문 유형이 단순한 앱은 3개로 충분하나, 문의 유형이 5종 이상으로 다양하면 5~10개로 늘림 — 개수만 늘고 절차는 같음.

**2단계 — 질문마다 위에서부터 예/아니오로 내려감.**

| 물음 | 예 | 아니오 |
|------|----|--------|
| 답이 조건에 맞는 **목록**인가(반경·필터·정렬) | **정형 조회**(검색 아님) | 다음 줄 |
| 답이 **개수·합계·추이**인가 | **NL2SQL** | 다음 줄 |
| 답이 문서 1~2군데 문장인가 | **벡터 RAG** | 다음 줄 |
| 관계를 2번 이상 건너가야 하나 | **GraphRAG** | 다음 줄 |
| 사내 코드·용어를 1:1로 고정해야 하나 | **용어사전·온톨로지** | `[확인필요]` |
| 이 판정이 틀리면 **되돌릴 수 없나** | 고른 경로 **앞에** 결정론 단계를 따로 둠 | 그대로 진행 |

용어 — NL2SQL은 말을 SQL로 바꿔 표를 조회함. 벡터 RAG는 뜻이 가까운 문단을 찾아옴.  
GraphRAG는 개체를 선으로 이어 그 선을 따라감.

**첫 줄이 왜 맨 위인가** — 데이터가 대부분 표인 앱은 답이 `목록`임. 이 줄이 없으면  
넷 다 아니오가 되어 전부 `[확인필요]`로 떨어짐.

**마지막 줄은 경로를 바꾸지 않음.** 알레르기·투약 판정은 확률에 맡기지 않음.  
결정론 단계를 앞에 두고 **LLM 입력 규격에서 그 필드를 지움.** 칸이 없으면 값이 갈 길이 없음.

**3단계 — 버린 대안 1줄.** "GraphRAG는 비용 때문에 뺌"처럼 남겨야 다시 판단 가능함.  
**멈춤 신호** — 표본 질문이 한 경로로만 몰리면 잘못 뽑은 것임. 다시 뽑은 뒤 시작함.

**이 판정은 최종 확정이 아님.** 표본 3~10개로 잡은 방향은 3절 골든셋(20~30문항)으로 다시 검증함.  
골든셋 통과율이 낮으면 이 절로 되돌아와 판정을 다시 함 — 3단계의 버린 대안 1줄이 재판단의 근거가 됨.

---

## 3. 설계항목 작성법

각 칸 첫 줄은 규격임 — **형태**(값의 모양) · **비면**(빈칸에 적을 값) ·  
**주인**(값을 가진 다른 산출물. 없으면 생략) · **대조**(다 채운 뒤 맞출 것).  
그 아래 대시 불렛이 채우는 요령임.  
설계항목은 `정형 → 비정형 → 커넥터 → 민감 → 품질` 계열 순으로 놓았음.

| 설계항목 | 무엇을 적나 | 어디서 가져오나 | 예시 |
|----|------------|----------------|---------|
| 정형 접근 경로 | **형태** 표 이름 하나씩 · 행 수 상한 숫자 · **비면** `[확인필요]` · **대조** ③ 패턴/시퀀스의 검색 단계와 경로 수 일치<br>- 조회할 표(테이블) 이름을 하나씩 적음<br>- 그 표에서 무엇을 얻으려는지 목적을 1줄로 적음<br>- 조건으로 거르는 값(반경·기간·상태 같은 것)이 있으면 함께 적음<br>- 읽기 전용 계정만 씀 — 넣기·바꾸기·지우기는 이 경로에 넣지 않음<br>- 한 번에 가져올 행 수 상한을 숫자로 적음. 못 정했으면 `[확인필요]`로 적음 | US `[입력 요구사항]` | `code_map` — 3방향 코드 조회 |
| 정형 접근 금지 컬럼 | **형태** `표.열` 꼴 하나씩 · **비면** `없음(전 항목 허용)` · **대조** US 보안 체크리스트의 금지 항목 전건 포함<br>- **정형 접근 경로(표)에만 적용됨** — 비정형 문서·커넥터 값의 제한은 이 항목이 아님<br>- 담당자가 못 보게 막을 열(컬럼)을 `표.열` 꼴로 하나씩 적음<br>- 막는 이유를 짧게 붙임(개인정보·계약금액 같은 것)<br>- 막을 것이 없어도 빈칸으로 두지 않고 `없음(전 항목 허용)`이라고 적음 | US 보안 체크리스트 | `cust.phone` |
| 비정형 경로 선택·정의 | **형태** 후보 방식 1행씩 · 채택·미채택 표기 · **비면** 미채택 `해당 없음` · 미정 `[확인필요]` · **대조** 행 수 = 후보 방식 수<br>- 문서에서 답을 찾는 방식을 왜 그것으로 골랐는지 1줄로 적음<br>- 검토한 후보 방식을 채택·미채택 모두 적음(벡터 RAG·GraphRAG·용어사전 등)<br>- 미채택 방식마다 버린 이유를 1줄로 적음 — "구축 비용이 MVP 기간을 넘김" 꼴. "요즘 많이 씀" 같은 유행은 근거로 쓰지 않음<br>- 채택한 방식마다 실제 구성 스펙을 적음 — 필드 목록은 표 아래 설명 참고<br>- 미채택 방식의 스펙 칸은 `해당 없음`으로 적음. 못 정한 칸은 `[확인필요]`로 적음 | 질문 유형 · 방식별 스펙은 직접 설계 | 표현이 달라 낱말 일치로 누락됨 |
| 커넥터 입출력 키 | **형태** 키 이름과 타입 · **비면** `[확인필요]` · **주인** ④ 역할계약서(⑤는 베낌) · **대조** ④의 키 이름과 문자열 일치<br>- 커넥터(외부 시스템에 붙는 도구)가 주고받는 값의 이름과 타입을 하나씩 적음<br>- 이름은 ④ 역할계약서에 이미 있는 것을 그대로 베낌 — ⑤ 지식/도구가 새 이름을 지어내지 않음(값의 주인은 ④ 역할계약서임)<br>- ④ 역할계약서의 입출력 형식 칸에 없으면 US `[처리 결과]`에서 가져옴<br>- ② 논리아키텍처에서 경계를 못 통과한 항목은 키 자체를 만들지 않음 — 가리는(마스킹) 것이 아니라 아예 빼는 것임<br>- 그 항목을 키로 넣어 두고 "안 씀"이라고만 적으면 안 됨 | ④ 역할계약서의 입출력 형식 칸 → 없으면 US `[처리 결과]` | `facilityId: string`(④ 역할계약서의 이름대로) |
| 커넥터 검증 기준 | **형태** 합격선 숫자 · 호출 순서 일치 여부 · **비면** `[확인필요]` · **대조** 커넥터 입출력 키 행 수와 같음<br>- 그 커넥터가 어느 선을 넘으면 합격인지 합격선을 숫자로 적음<br>- 부르는 순서까지 맞아야 정답으로 볼 것인지 함께 적음 — 순서가 틀리면 0점임(A08)<br>- "잘 붙으면 됨"처럼 재는 방법이 없는 말은 쓰지 않음 | A08 지표 | 호출 순서까지 일치 시 정답 |
| Mock·실물 구분 | **형태** 커넥터마다 `Mock`·`실물` 하나 · **비면** `[확인필요]` · **주인** ② 논리아키텍처 소유 · **대조** ② 논리아키텍처와 값 일치<br>- 커넥터마다 실제 시스템에 붙는지, 흉내만 내는 가짜(Mock)인지 하나씩 적음<br>- 값은 ② 논리아키텍처에 있는 것을 그대로 베낌 — 여기서 새로 정하지 않음<br>- ② 논리아키텍처와 값이 다르면 ② 논리아키텍처를 따르고, 다르다는 사실만 1행 남김 | **② 논리아키텍처가 값을 소유** | ② 논리아키텍처와 같음(`Mock`) |
| 커넥터 **사용 주체** | **형태** ③ 패턴/시퀀스의 단계 · ④ 역할계약서의 담당자 · **비면** `[확인필요]` · **대조** ③ 패턴/시퀀스 단계 이름과 문자열 일치<br>- 그 커넥터를 누가 부르는지 적음 — ③ 패턴/시퀀스의 어느 단계, ④ 역할계약서의 어느 담당자인지<br>- 사람 요청 경로와 뒤에서 도는 배치 경로가 같은 시스템을 쓰면 둘 다 적음<br>- 빈칸으로 두면 ④ 역할계약서는 요청 경로로만 짐작하고 ⑦ 배포는 접속 정보를 둘 곳을 못 정함 | ③ 패턴/시퀀스의 단계 · ④ 역할계약서의 사용 도구 | `동기화 워커·길찾기만(요청은 캐시)` |
| 민감 필드 ID(`F-n`) | **형태** `F-1`·`F-2` 연번 · **주인** ⑤ 지식/도구 소유(④는 인용만) · **대조** 민감정보 목록 행 수 이상<br>- 민감 필드마다 `F-1`·`F-2`처럼 번호를 붙임 — 번호의 주인은 ⑤ 지식/도구임<br>- ④ 역할계약서는 이 번호를 가져다 인용만 함. 둘이 각자 따로 번호를 붙이지 않음<br>- ④ 역할계약서가 여기 없던 필드를 찾으면 기존 번호는 그대로 두고 뒤에 새 번호를 더함 | **⑤ 지식/도구가 값 소유 · ④ 역할계약서는 인용** | `F-2 = 좌표(⑤ 지식/도구 기준)` |
| 민감 필드 변환 지점 | **형태** `SL-n` 식별자 + 가리는 방법 · **비면** `[확인필요]` · **대조** `F-n` 행마다 1개 · ② 논리아키텍처 식별자 문자열 포함<br>- 민감한 값을 가리는 자리를 ② 논리아키텍처의 경계 식별자(`SL-n`)로 콕 집어 적음<br>- 어떻게 가리는지도 함께 적음(별표 처리·해시로 바꾸기·아예 제외)<br>- 식별자 문자열이 칸에 그대로 들어가야 함 — "서비스에서 적절히"처럼 자리를 안 가리키는 말은 안 됨 | ② 논리아키텍처의 민감정보 라벨 식별자 | `SL-2 모델 직전` |
| 보존·삭제 | **형태** 보존 기간 숫자 + 단위 · 삭제 주기 · **비면** `[확인필요]` · **대조** 기획 자료 출처 문구가 같은 칸에 있음<br>- 그 데이터를 얼마나 오래 두는지 기간을 숫자로 적음<br>- 지우는 방식과 주기를 적음(월 1회 일괄 파기 같은 것)<br>- 그 기간을 기획 자료에서 인용하고 출처를 함께 적음 — "회사 규정대로"는 출처가 아님 | US 컴플라이언스 | 개통 완료 후 3년 |
| 원천 오류율 | **형태** `오류율 % / 측정법 / 측정일` 3칸 · **비면** 원천 없음 `경로 불가` · 미측정 `[확인필요]` · **대조** 경로 행 수와 같음<br>- 경로마다 3칸을 적음 — `원천 오류율 / 측정 방법 / 측정일`<br>- 그 원천이 실제로 있는지 먼저 확인함 — 없으면 오류율이 아니라 `경로 불가`라고 적음<br>- 재 본 적이 없으면 `[확인필요]`로 둠. 짐작한 숫자를 쓰지 않음<br>- 캐시를 거치는 경로면 `갱신 지연` 1칸을 더 적음 | 기획 자료의 품질 수치 → 없으면 실측 | `25~30% / 기획서 인용 / 2026-08-01` |
| 골든셋 문항 | **형태** 문항 수 숫자 + 유형별 배분 · **비면** `[확인필요]` · **대조** 20문항 이상 · ① 목표/품질 카드의 비율 목표 산식<br>- 골든셋(정답지)을 몇 문항 만들지 숫자로 적음 — 20 ~ 30문항이 기준임<br>- 2절 판정표의 유형 4종을 고르게 섞은 배분을 적음(집계 10 · 코드 조회 10 · 해석 10 꼴)<br>- 문항마다 `질문 / 정답 / 근거 문서·행`을 적음<br>- 검색을 고치기 전에 먼저 만듦 — 순서가 반대면 고쳐졌는지 알 수 없음 | 문의 로그 → 없으면 US `[검증 요구사항]` | `30문항 — 집계 10·코드 10·해석 10` |

**비정형 경로 선택·정의 — 방식별 필수 스펙**  
"정형 접근 경로"는 표 이름·필터·상한까지 구체값을 적지만, 비정형 경로는 선택 근거만 적고 실제 구성은 비워두기 쉬움.  
채택한 방식마다 아래 스펙을 채움. 미채택 방식은 `해당 없음`으로 적음.

| 채택 방식 | 필수 스펙 필드 | 없으면 생기는 사고 |
|-----------|----------------|-------------------|
| 벡터 RAG | 인덱스명 · 문서 범위·기준일 · 청킹 단위/크기/중첩 · 임베딩 모델명·버전 · 검색방식·top-k·리랭킹 여부 · 메타데이터 필터 키 | 재색인 때 값이 달라져 이전 평가와 비교 불가함 · 권한 경계를 못 걸러 못 볼 문서가 근거로 인용됨 |
| GraphRAG | 노드 타입 목록 · 관계(엣지) 타입·방향 · 노드 고유키 규칙 · 추출·검수 방법 · 최대 홉 수·결과 상한 · 커뮤니티 요약 사용 여부 | 부정 관계(`선택 안 함`)가 뭉개져 정반대 답이 나옴 · 재적재 때 노드가 중복으로 계속 불어남 |
| 온톨로지·용어사전 | 사전 파일 위치·형식 · 대표어·동의어 목록 · 적용 지점(질의 확장/검색 후/출력 표기) · 1:N 충돌 처리 규칙 · 미등록어 처리 규칙 · 갱신 주체·주기 | 코드가 두 뜻일 때 오류 없이 조용히 틀림 · 신규 코드 미반영을 검색 품질 문제로 오진함 |

메타데이터 필터 키는 ④ 역할계약서에 있는 이름을 인용만 함 — ⑤ 지식/도구가 새 이름을 짓지 않음(G-12).

**접근 제한 3종의 경계** — 이름이 비슷해 헷갈리기 쉬움. `정형 접근 금지 컬럼`(표.열, 아예 조회 안 함) ·  
`메타데이터 필터 키`(방식별 스펙 표, 비정형 문서 중 못 볼 것을 걸러냄) · `민감 필드 변환 지점`(설계항목 표, 조회는 되나 값을 가림)  
— 셋은 막는 층이 다름. 하나로 합쳐 적지 않음.

**G-10 확정** — Mock 구분 목록은 ⑤ 지식/도구에 두되 값의 주인은 ② 논리아키텍처임. ⑤ 지식/도구는 커넥터 단위로 펼쳐 적고,  
값이 다르면 ② 논리아키텍처를 따르고 다르다는 사실만 1행 남김.

**소유 방향이 서로 반대임** — 필드 ID는 **⑤ 지식/도구**가, 키 이름은 **④ 역할계약서**가 가짐(G-12).  
단계 간 **키 집합 식별자**는 ④ 역할계약서보다 먼저 쓰는 **③ 패턴/시퀀스**가 가짐 — 셋이 각자 다른 층을 소유함.  
③ 패턴/시퀀스·④ 역할계약서의 키가 이상해도 ⑤ 지식/도구에서 고치지 않고 `정합 확인 필요` 칸에만 적음.

**사용 주체 칸** — 같은 외부 시스템을 요청 경로와 배치 경로가 함께 씀. 안 적으면  
④ 역할계약서는 요청 경로에 넣고 ⑦ 배포는 접속 정보를 둘 곳을 못 정함.

**원천 오류율 칸 — 2단으로 물음**  
검색을 손봐도 원본이 틀리면 답도 틀림. 순서가 있음.

**1단 그 원천이 실제로 있나** — 아니오면 오류율이 아니라 `경로 불가`임.  
없는 원천에 오류율을 채우려다 막히는 것이 가장 흔한 사고임.  
**2단 오류율을 쟀나** — 경로별로 `원천 오류율 / 측정 방법 / 측정일` 3칸을 더함.  
못 쟀으면 `[확인필요]`로 둠. 짐작한 숫자를 쓰지 않음.  
캐시를 거치면 `갱신 지연` 1칸을 더함 — 실제 오류율에 지연분이 얹힘.

**골든셋 20~30문항**  
정답을 먼저 만들고 검색을 고침. 순서가 반대면 고쳤는지 알 수 없음.  
만드는 법 — 문의 로그에서 자주 나온 순으로 뽑음 → 유형 4종을 고르게 섞음 →  
문항마다 `질문 / 정답 / 근거 문서·행`을 적음 → 2명이 따로 풀어 답이 갈리면 버림.

**로그가 없는 신규 서비스** — 미출시면 로그가 0건임. 대신 유저스토리의  
`[검증 요구사항]` 1항목을 문항 1개로 바꿈. 그것이 곧 정답 조건임.  
Pain Point 표를 빈도 순 후보로 쓰고, 베타 뒤 실제 로그로 바꿈.

**비율 목표는 표본 수를 먼저 셈** — 목표가 `95%`면 필요 표본은 `1 ÷ (1 − 0.95) = 20`개임.  
6문항(태그 18개)이면 눈금이 5.56%p라 `17/18 = 94.4%`가 불합격임. 합격선이 사실상 100%가 됨.  
① 목표/품질 카드의 비율 목표마다 이 산식으로 문항 수를 정함.

> **경고** — 공개 실습 데이터 BookSQL은 `CC BY-NC-SA 4.0` 비상업 라이선스임.
> 유상 교육·사내 과제에 원본을 그대로 쓰지 않음. **자기 도메인 30문항**으로 대체함.

**되돌리기 왕복 1회로 끝내는 규칙**

순서 — `⑤ 지식/도구의 필드 초안 → ④ 역할계약서의 권한 배정 → ⑤ 지식/도구의 변환 지점 확정 →`  
`③ 패턴/시퀀스의 커넥터 규격 되돌림 확인(C-2) → 종료`.  
③ 패턴/시퀀스는 ⑤ 지식/도구보다 먼저 쓰였으므로 마지막 칸은 **새로 쓰는 것이 아니라 이미 쓰인 값과 맞추는 확인**임.  
④ 역할계약서가 ⑤ 지식/도구에 없던 필드를 찾으면 ⑤ 지식/도구가 번호를 **뒤에** 더함. 기존 번호를 다시 매기지 않음.  
중단 기준 3가지 — 왕복 2회째 / 같은 칸 세 번째 수정 / 30분 초과.  
하나라도 걸리면 멈추고 `[확인필요]`로 남긴 뒤 넘어감.

---

## 4. 제출 전 자가 점검표

- [ ] 정형 접근 경로 표에 데이터 행이 1행 이상 있고 `정형 접근 금지 컬럼` 공란이 0건임
      (막을 것이 없으면 `없음(전 항목 허용)`)
- [ ] 비정형 경로 표의 행 수 = **후보 방식 수**(채택·미채택 표기 포함)
- [ ] 채택한 비정형 방식마다 스펙 칸이 채워져 있음(벡터 RAG=인덱스·청킹·임베딩 모델, GraphRAG=노드·관계 타입, 용어사전=파일 위치·매핑 규칙).
      미채택 방식은 `해당 없음`
- [ ] 커넥터 입출력 키가 ④ 역할계약서의 입출력 형식 칸의 이름과 어긋난 건수 0건
- [ ] ② 논리아키텍처의 `경계 미통과 항목`이 커넥터 입출력 키에 **하나도 없음**(마스킹 처리가 아니라 키 자체를 뺐는지 확인)
- [ ] 경로마다 `버린 대안` 1줄이 있음
- [ ] 커넥터 표의 입력·출력·Mock 구분 3칸에 공란이 0건임
- [ ] 민감 필드 표 행 수 ≥ 민감정보 목록 행 수
- [ ] `변환 수행 지점` 칸 전부에 ② 논리아키텍처의 경계 식별자 문자열이 들어감
- [ ] 보존 기간·삭제 주기 숫자를 기획 자료에서 인용했고 출처를 적음
- [ ] 원천 오류율 칸이 있고, 못 쟀으면 `[확인필요]`로 적혀 있음
- [ ] 골든셋 문항 수가 20 이상이고 유형 4종이 섞여 있음
- [ ] 쓰기 SQL 금지가 문장으로 있고, 행 수 상한이 숫자이거나 `[확인필요]`로 되물어진 상태임
- [ ] `[확인필요]` 전부가 말미 미확정 항목 표에 1행씩 있음

---

## 5. 다음으로 넘기는 값과 근거 자료

③ 패턴/시퀀스·④ 역할계약서는 ⑤ 지식/도구보다 앞에 있으므로 아래 `C-1`·`C-2` 두 줄은 **앞으로 넘기는 값이 아니라 되돌리는 값**임.

| 넘기는 값 | 받는 곳 | 강도 |
|-----------|---------|------|
| 접근 경로 목록 | ③ 패턴/시퀀스의 검색 단계(되돌림) | C-2 |
| 커넥터 규격 · 사용 주체 | ③ 패턴/시퀀스의 단계 간 데이터 형식(되돌림) | C-2 |
| 민감 필드 분류표 · `F-n` ID | ④ 역할계약서의 접근 가능한 정보 항목(되돌림) | C-1 |
| 저장소 종류 | ⑦ 배포의 배치 위치 | HARD |
| 검색 품질 지표 · 문항 수 | ⑥ 가드레일/관측의 품질 측정 대상 | 참조 |

| 아티클 | 거는 칸 |
|--------|---------|
| [B01](../references/articles/B01_book_essential-graphrag.md) | 비정형 경로 근거 · 청킹 기본값 · GraphRAG 비용 |
| [A09](../references/articles/A09_tool_ragas-text2sql.md) | NL2SQL 평가 · 골든셋 라이선스 주의 |
| [A08](../references/articles/A08_tool_ragas-agent-metrics.md) | 커넥터 검증 — 순서가 틀리면 0점 |
| [A06](../references/articles/A06_std_owasp-agentic-top10.md) | `정형 접근 금지 컬럼` 칸의 위협 근거 |
| [A10](../references/articles/A10_reg_fsc-ai-guideline.md) | 금융 도메인 민감 필드 처리 근거 |

**A10 주의** — `최소수집`·`비식별` 2칸은 근거가 없음. 못 찾으면 `[확인필요]`임.
